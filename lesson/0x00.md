### Пример 1: Извлечение метода (Extract Method)

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int a = 5, b = 10;
    int sum = a + b;
    int diff = a - b;
    int prod = a * b;
    int quot = a / b;
    int rem = a % b;

    printf("Sum: %d\n", sum);
    printf("Difference: %d\n", diff);
    printf("Product: %d\n", prod);
    printf("Quotient: %d\n", quot);
    printf("Remainder: %d\n", rem);

    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void perform_operations(int a, int b) {
    printf("Sum: %d\n", a + b);
    printf("Difference: %d\n", a - b);
    printf("Product: %d\n", a * b);
    printf("Quotient: %d\n", a / b);
    printf("Remainder: %d\n", a % b);
}

int main() {
    int a = 5, b = 10;
    perform_operations(a, b);
    return 0;
}
```

**Описание техники:**

Техника "Извлечение метода" (Extract Method) заключается в выделении части кода, выполняющей отдельную задачу, в отдельную функцию. Это улучшает читаемость, повторное использование и тестируемость кода. В данном примере операции над числами были вынесены в функцию `perform_operations`, что делает основной код более чистым и понятным.([volito.digital][1])

**Оценка качества кода:**

* До рефакторинга: 6/10 — код выполняет свою задачу, но неструктурирован и труден для расширения.
* После рефакторинга: 9/10 — код стал более модульным, читаемым и легко расширяемым.

---

### Пример 2: Переименование переменных (Rename Variable)

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int a = 100;
    int b = 200;
    int c = a + b;
    printf("Result: %d\n", c);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int first_number = 100;
    int second_number = 200;
    int sum = first_number + second_number;
    printf("Sum: %d\n", sum);
    return 0;
}
```

**Описание техники:**

Техника "Переименование переменных" (Rename Variable) улучшает читаемость кода, делая имена переменных более описательными. В данном примере переменные `a`, `b` и `c` были переименованы в `first_number`, `second_number` и `sum` соответственно, что делает код более понятным для других разработчиков.

**Оценка качества кода:**

* До рефакторинга: 5/10 — имена переменных не информативны, что затрудняет понимание кода.
* После рефакторинга: 8/10 — имена переменных стали более описательными, улучшив читаемость.([forkful.ai][2])

---

### Пример 3: Замена магического числа на константу (Replace Magic Number with Constant)

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int radius = 5;
    double area = 3.14159 * radius * radius;
    printf("Area: %.2f\n", area);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define PI 3.14159

int main() {
    int radius = 5;
    double area = PI * radius * radius;
    printf("Area: %.2f\n", area);
    return 0;
}
```

**Описание техники:**

Техника "Замена магического числа на константу" (Replace Magic Number with Constant) заключается в замене числовых значений, не имеющих явного смысла, на именованные константы. Это улучшает читаемость и облегчает изменение значений в будущем. В данном примере число `3.14159` было заменено на константу `PI`, что делает код более понятным и удобным для изменения.

**Оценка качества кода:**

* До рефакторинга: 6/10 — магическое число затрудняет понимание кода и его поддержку.
* После рефакторинга: 9/10 — использование константы улучшает читаемость и облегчает поддержку кода.

---

### Пример 4: Удаление дублирования кода (Remove Duplicate Code)

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 5, y = 10;
    int sum1 = x + y;
    int diff1 = x - y;
    printf("Sum1: %d, Diff1: %d\n", sum1, diff1);

    x = 15, y = 20;
    int sum2 = x + y;
    int diff2 = x - y;
    printf("Sum2: %d, Diff2: %d\n", sum2, diff2);

    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_operations(int x, int y) {
    printf("Sum: %d, Difference: %d\n", x + y, x - y);
}

int main() {
    print_operations(5, 10);
    print_operations(15, 20);
    return 0;
}
```

**Описание техники:**

Техника "Удаление дублирования кода" (Remove Duplicate Code) заключается в выявлении повторяющихся фрагментов кода и их объединении в общие функции. Это снижает объем кода, облегчает его поддержку и уменьшает вероятность ошибок. В данном примере повторяющиеся операции сложения и вычитания были вынесены в функцию `print_operations`, что устраняет дублирование и делает код более компактным.

**Оценка качества кода:**

* До рефакторинга: 5/10 — дублирование кода усложняет его поддержку и увеличивает вероятность ошибок.
* После рефакторинга: 8/10 — удаление дублирования улучшает структуру кода и облегчает его поддержку.

---

### Пример 5: Инлайн метода (Inline Method)

**До рефакторинга:**

```c
#include <stdio.h>

int get_square(int x) {
    return x * x;
}

int main() {
    int num = 4;
    printf("Square: %d\n", get_square(num));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int num = 4;
    printf("Square: %d\n", num * num);
    return 0;
}
```

**Описание техники:**

Техника "Инлайн метода" (Inline Method) заключается в замене вызова метода его содержимым, если метод слишком прост и не добавляет значимой абстракции. Это уменьшает количество кода и повышает его читаемость. В данном примере метод `get_square` был заменен непосредственно вычислением квадрата числа в функции `main`, что делает код более прямолинейным.

**Оценка качества кода:**

* До рефакторинга: 7/10 — метод добавляет абстракцию, но его использование может быть избыточным для такой простой операции.
* После рефакторинга: 8/10 — удаление метода делает код более прямолинейным и понятным.

---

## 🔧 **Пример 6: Удаление мёртвого кода (Remove Dead Code)**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 10;
    int y = 20;
    int z = x + y;

    // Неиспользуемый код
    int unused = 100;
    if (0) {
        printf("This will never run\n");
    }

    printf("Sum: %d\n", z);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 10;
    int y = 20;
    int z = x + y;

    printf("Sum: %d\n", z);
    return 0;
}
```

**Описание техники:**

Удаление мёртвого кода — это удаление фрагментов, которые не выполняются или не влияют на поведение программы. В данном примере переменная `unused` и условный блок `if (0)` никогда не используются и могут быть безопасно удалены. Это улучшает читаемость и снижает когнитивную нагрузку, а также может помочь компилятору лучше оптимизировать код.

**Оценка:**

* До: 5/10 — лишний шум, который может сбить с толку.
* После: 9/10 — код стал чище, без ненужных деталей.

---

## 🔧 **Пример 7: Использование условного оператора (Use Ternary Operator)**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int a = 10, b = 20;
    int max;

    if (a > b) {
        max = a;
    } else {
        max = b;
    }

    printf("Max: %d\n", max);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int a = 10, b = 20;
    int max = (a > b) ? a : b;

    printf("Max: %d\n", max);
    return 0;
}
```

**Описание техники:**

Условный (тернарный) оператор `?:` позволяет сократить простые конструкции `if-else`, особенно при присваивании значений. Это делает код короче и в ряде случаев более читаемым. Однако следует быть осторожным и не применять его к сложным выражениям, чтобы не ухудшить понимание.

**Оценка:**

* До: 7/10 — читаемо, но избыточно.
* После: 9/10 — лаконично и понятно.

---

## 🔧 **Пример 8: Инициализация в месте объявления (Initialize Variables Where Declared)**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int a;
    int b;
    a = 3;
    b = 4;
    printf("a + b = %d\n", a + b);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int a = 3;
    int b = 4;
    printf("a + b = %d\n", a + b);
    return 0;
}
```

**Описание техники:**

Инициализация переменных как можно ближе к месту использования — один из принципов чистого кода. Это уменьшает количество строк, упрощает чтение и уменьшает вероятность ошибок, связанных с неинициализированными значениями.

**Оценка:**

* До: 6/10 — рабочий, но неаккуратный стиль.
* После: 9/10 — ясность и компактность.

---

## 🔧 **Пример 9: Избегание повторных вызовов функции (Store Function Result)**

**До рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello";
    if (strlen(str) > 0) {
        printf("Length is %lu\n", strlen(str));
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello";
    size_t len = strlen(str);
    if (len > 0) {
        printf("Length is %lu\n", len);
    }
    return 0;
}
```

**Описание техники:**

Повторный вызов функции с одинаковыми параметрами — это анти-паттерн, особенно если функция не дешевая. Вынесение результата в переменную улучшает производительность и делает код понятнее.

**Оценка:**

* До: 6/10 — допустимо, но не оптимально.
* После: 9/10 — экономия ресурсов и улучшение читаемости.

---

## 🔧 **Пример 10: Избавление от вложенности (Reduce Nesting)**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 5;
    if (x > 0) {
        if (x < 10) {
            printf("x is between 1 and 9\n");
        }
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 5;
    if (x > 0 && x < 10) {
        printf("x is between 1 and 9\n");
    }
    return 0;
}
```

**Описание техники:**

Глубокая вложенность ухудшает читаемость кода. Объединение условий или использование логических операторов позволяет упростить структуру и сделать код более линейным.

**Оценка:**

* До: 6/10 — читается тяжеловато.
* После: 9/10 — лучше структурировано.

---

## 🔧 **Пример 11: Избавление от флага с досрочным выходом**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int found = 0;
    for (int i = 0; i < 10; ++i) {
        if (i == 5) {
            found = 1;
        }
    }

    if (found) {
        printf("Found 5\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    for (int i = 0; i < 10; ++i) {
        if (i == 5) {
            printf("Found 5\n");
            break;
        }
    }
    return 0;
}
```

**Описание:**
Удаление переменной-флага улучшает читаемость и упрощает логику — вместо того чтобы использовать дополнительную переменную, используется досрочный выход (`break`). Это делает поведение кода более прямым.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 12: Явное приведение типов**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 5;
    float y = x / 2;
    printf("%f\n", y);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 5;
    float y = (float)x / 2;
    printf("%f\n", y);
    return 0;
}
```

**Описание:**
Явное приведение типа гарантирует правильное поведение при делении: без него происходит целочисленное деление. Этот рефакторинг устраняет потенциальную ошибку и делает намерения программиста явными.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 13: Использование массива вместо отдельных переменных**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int a1 = 10, a2 = 20, a3 = 30;
    printf("%d %d %d\n", a1, a2, a3);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int a[] = {10, 20, 30};
    for (int i = 0; i < 3; ++i) {
        printf("%d ", a[i]);
    }
    printf("\n");
    return 0;
}
```

**Описание:**
Использование массива вместо множества переменных упрощает масштабируемость кода и позволяет легко применять алгоритмы (сортировку, поиск и т.п.).

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 14: Разделение длинной строки**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    printf("This is a very very very very very very very very long line of text that should be broken.\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    printf("This is a very very very very very "
           "very very very very long line of text "
           "that should be broken.\n");
    return 0;
}
```

**Описание:**
Длинные строки ухудшают читаемость и могут вызвать проблемы в некоторых системах. Разбиение строки делает код легче воспринимаемым и редактируемым.

**Оценка:**

* До: 5/10
* После: 8/10

---

## 🔧 **Пример 15: Минимизация области видимости переменной**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int i;
    for (i = 0; i < 5; ++i) {
        printf("%d\n", i);
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    for (int i = 0; i < 5; ++i) {
        printf("%d\n", i);
    }
    return 0;
}
```

**Описание:**
Ограничение области действия переменной уменьшает риск непреднамеренного её использования. Это улучшает читаемость и уменьшает количество ошибок.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 16: Объединение инициализации и объявления**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int sum;
    sum = 0;
    printf("%d\n", sum);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int sum = 0;
    printf("%d\n", sum);
    return 0;
}
```

**Описание:**
Объединение объявления и инициализации делает код компактнее и облегчает его понимание, особенно в контексте простых значений.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 17: Удаление лишнего комментария**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 5; // x is 5
    printf("%d\n", x);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 5;
    printf("%d\n", x);
    return 0;
}
```

**Описание:**
Комментарии, повторяющие то, что очевидно из кода — это "шум". Удаление таких комментариев помогает сделать важные комментарии заметнее и улучшает читаемость.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 18: Использование `const` для неизменяемых значений**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int limit = 100;
    printf("Limit is %d\n", limit);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    const int limit = 100;
    printf("Limit is %d\n", limit);
    return 0;
}
```

**Описание:**
`const` помогает компилятору и другим разработчикам понять, что переменная не должна изменяться. Это улучшает безопасность и читабельность.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 19: Использование предопределённых макросов**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    printf("This is version 1.0\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define VERSION "1.0"

int main() {
    printf("This is version %s\n", VERSION);
    return 0;
}
```

**Описание:**
Использование макросов (или `const char*` в более сложных случаях) позволяет централизованно менять значения, избегая ошибок при множественном использовании.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 20: Проверка результата функции**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    printf("Hello\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    if (printf("Hello\n") < 0) {
        return 1;
    }
    return 0;
}
```

**Описание:**
Проверка результата функций (особенно ввода/вывода) — хороший стиль, особенно в системном программировании. Это делает программу надёжнее и легче отлаживаемой.

**Оценка:**

* До: 4/10
* После: 8/10

---

## 🔧 **Пример 21: Объединение повторяющихся `printf` с шаблоном**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    printf("Name: Alice\n");
    printf("Age: 30\n");
    printf("Country: USA\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_info(const char* label, const char* value) {
    printf("%s: %s\n", label, value);
}

int main() {
    print_info("Name", "Alice");
    print_info("Age", "30");
    print_info("Country", "USA");
    return 0;
}
```

**Описание:**
Повторяющиеся вызовы `printf` с похожими шаблонами могут быть обобщены через вспомогательную функцию. Это упрощает поддержку и делает структуру кода более явной.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 22: Удаление глобальной переменной**

**До рефакторинга:**

```c
#include <stdio.h>

int counter = 0;

void increment() {
    counter++;
}

int main() {
    increment();
    printf("%d\n", counter);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int increment(int c) {
    return c + 1;
}

int main() {
    int counter = 0;
    counter = increment(counter);
    printf("%d\n", counter);
    return 0;
}
```

**Описание:**
Глобальные переменные увеличивают связанность и затрудняют тестирование. Передача состояния через параметры делает код более модульным и предсказуемым.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 23: Использование `enum` вместо числовых литералов**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int status = 1; // 0 - OK, 1 - ERROR
    if (status == 1) {
        printf("Error\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef enum { OK, ERROR } Status;

int main() {
    Status status = ERROR;
    if (status == ERROR) {
        printf("Error\n");
    }
    return 0;
}
```

**Описание:**
Использование `enum` вместо "магических чисел" повышает читаемость и снижает вероятность логических ошибок, особенно в условиях и логике состояний.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 24: Замена рекурсии на итерацию (хвостовая рекурсия)**

**До рефакторинга:**

```c
#include <stdio.h>

int factorial(int n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);
}

int main() {
    printf("%d\n", factorial(5));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int factorial(int n) {
    int result = 1;
    for (int i = 2; i <= n; ++i) {
        result *= i;
    }
    return result;
}

int main() {
    printf("%d\n", factorial(5));
    return 0;
}
```

**Описание:**
Рекурсия удобна, но в C часто заменяется на итерацию ради безопасности стека. Такой подход особенно полезен при работе с большими входами.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 25: Использование `fgets` вместо `gets`**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    char name[100];
    gets(name);
    printf("Hello, %s\n", name);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    char name[100];
    fgets(name, sizeof(name), stdin);
    printf("Hello, %s", name);
    return 0;
}
```

**Описание:**
`gets` удалён из стандарта C из-за угрозы переполнения буфера. `fgets` безопаснее, так как позволяет задать размер ввода. Это критически важно для надёжности программ.

**Оценка:**

* До: 2/10
* После: 10/10

---

## 🔧 **Пример 26: Избавление от "магии" в цикле**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    for (int i = 0; i < 7; ++i) {
        printf("Day %d\n", i);
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define DAYS_IN_WEEK 7

int main() {
    for (int i = 0; i < DAYS_IN_WEEK; ++i) {
        printf("Day %d\n", i);
    }
    return 0;
}
```

**Описание:**
Число `7` имеет смысл только в контексте. Использование `#define` или `const` позволяет читателю понять, что именно означает эта константа и зачем она используется.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 27: Упрощение логических выражений**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 1;
    if (x == 1) {
        printf("Yes\n");
    } else {
        printf("No\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 1;
    printf("%s\n", x == 1 ? "Yes" : "No");
    return 0;
}
```

**Описание:**
Простое условие может быть выражено тернарным оператором, особенно если оба варианта — вызовы одной функции с разными параметрами.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 28: Использование `sizeof(var)` вместо `sizeof(type)`**

**До рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int* arr = malloc(10 * sizeof(int));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int* arr = malloc(10 * sizeof(*arr));
    return 0;
}
```

**Описание:**
`sizeof(*arr)` безопаснее: при изменении типа указателя `sizeof` автоматически подстраивается. Это снижает количество багов при рефакторинге типов.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 29: Удаление дублирующей логики из `if`-блоков**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 1;
    if (x) {
        printf("x is non-zero\n");
        printf("Done\n");
    } else {
        printf("x is zero\n");
        printf("Done\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 1;
    if (x) {
        printf("x is non-zero\n");
    } else {
        printf("x is zero\n");
    }
    printf("Done\n");
    return 0;
}
```

**Описание:**
Общий код можно вынести из `if`/`else`. Это уменьшает дублирование и упрощает изменение общего поведения без ошибок.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 30: Использование `do { } while (0)` в макросе**

**До рефакторинга:**

```c
#define LOG(msg) printf("LOG: %s\n", msg)

int main() {
    if (1)
        LOG("Success");
    else
        printf("Fail\n");
    return 0;
}
```

**После рефакторинга:**

```c
#define LOG(msg) do { printf("LOG: %s\n", msg); } while (0)

int main() {
    if (1)
        LOG("Success");
    else
        printf("Fail\n
```

---

## 🔧 **Пример 31: Удаление ненужной проверки**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 10;
    if (x != 0) {
        printf("x is %d\n", x);
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int x = 10;
    printf("x is %d\n", x);
    return 0;
}
```

**Описание:**
Если значение гарантированно отлично от 0 или любая проверка бессмысленна в текущем контексте — её можно удалить. Это избавляет от лишней логики и делает код линейным и ясным.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 32: Разделение функции, делающей слишком много**

**До рефакторинга:**

```c
#include <stdio.h>

void process() {
    int x = 10;
    printf("Value: %d\n", x);
    x *= 2;
    printf("Doubled: %d\n", x);
}

int main() {
    process();
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_value(int x) {
    printf("Value: %d\n", x);
}

void print_doubled(int x) {
    printf("Doubled: %d\n", x * 2);
}

int main() {
    int x = 10;
    print_value(x);
    print_doubled(x);
    return 0;
}
```

**Описание:**
Функции должны выполнять одну логическую задачу. Разделение по смыслу помогает улучшить читаемость, переиспользуемость и тестируемость.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 33: Удаление временной переменной**

**До рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello";
    int len = strlen(str);
    printf("Length: %d\n", len);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello";
    printf("Length: %lu\n", strlen(str));
    return 0;
}
```

**Описание:**
Если переменная используется один раз и не нужна для ясности — её можно опустить. Особенно это актуально для простых выражений.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 34: Использование `switch` вместо `if-else` цепочки**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int code = 2;
    if (code == 1)
        printf("One\n");
    else if (code == 2)
        printf("Two\n");
    else if (code == 3)
        printf("Three\n");
    else
        printf("Unknown\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int code = 2;
    switch (code) {
        case 1: printf("One\n"); break;
        case 2: printf("Two\n"); break;
        case 3: printf("Three\n"); break;
        default: printf("Unknown\n");
    }
    return 0;
}
```

**Описание:**
`switch` лучше подходит для работы с множеством вариантов, чем длинная цепочка `if-else`. Он более читаем, расширяем и оптимизируется компилятором.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 35: Вынесение магического числа в `#define`**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int size = 1024;
    printf("Size: %d\n", size);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define BUFFER_SIZE 1024

int main() {
    int size = BUFFER_SIZE;
    printf("Size: %d\n", size);
    return 0;
}
```

**Описание:**
Магические числа усложняют понимание. Использование именованных макросов делает код самодокументируемым и облегчает изменение значения.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 36: Использование булевского типа (`stdbool.h`)**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int done = 0;
    if (!done) {
        printf("Not done\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdbool.h>

int main() {
    bool done = false;
    if (!done) {
        printf("Not done\n");
    }
    return 0;
}
```

**Описание:**
Использование типа `bool` вместо `int` в логических выражениях делает намерения яснее и упрощает понимание кода. Это особенно важно в командах.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 37: Присваивание по условию вместо `if`**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    int a = 5, b = 10;
    int max;
    if (a > b) {
        max = a;
    } else {
        max = b;
    }
    printf("Max: %d\n", max);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    int a = 5, b = 10;
    int max = (a > b) ? a : b;
    printf("Max: %d\n", max);
    return 0;
}
```

**Описание:**
Когда присваивание зависит от условия, тернарный оператор позволяет значительно сократить код без потери читаемости.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 38: Явная проверка на `NULL`**

**До рефакторинга:**

```c
#include <stdio.h>

void print(const char* msg) {
    printf("%s\n", msg);
}

int main() {
    char* ptr = NULL;
    print(ptr);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print(const char* msg) {
    if (msg)
        printf("%s\n", msg);
    else
        printf("(null)\n");
}

int main() {
    char* ptr = NULL;
    print(ptr);
    return 0;
}
```

**Описание:**
Обращение к `NULL` может привести к сбою. Проверка входных параметров обязательна, особенно в публичных API.

**Оценка:**

* До: 3/10
* После: 10/10

---

## 🔧 **Пример 39: Использование `const char*` вместо `char*` для строк литералов**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    char* msg = "Hello";
    printf("%s\n", msg);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    const char* msg = "Hello";
    printf("%s\n", msg);
    return 0;
}
```

**Описание:**
Строковые литералы неизменяемы. Использование `const` защищает от случайных изменений и предотвращает неопределённое поведение.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 40: Удаление ненужного `return` в `main()`**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    printf("Done\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Done\n");
}
```

**Описание:**
В стандарте C99 и выше `main` может завершаться без явного `return 0;`. Это делает код чуть короче и не влияет на поведение.

**Оценка:**

* До: 8/10
* После: 9/10


---

## 🔧 **Пример 41: Использование `sizeof(var)` для динамического массива**

**До рефакторинга:**

```c
#include <stdlib.h>

int main() {
    int* numbers = (int*)malloc(10 * sizeof(int));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdlib.h>

int main() {
    int* numbers = malloc(10 * sizeof(*numbers));
    return 0;
}
```

**Описание:**
Использование `sizeof(*var)` делает код более надёжным при изменении типа переменной. Это исключает ошибки, когда тип и размер могут рассинхронизироваться.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 42: Удаление `#include`, который не используется**

**До рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main() {
    printf("Hello\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    printf("Hello\n");
    return 0;
}
```

**Описание:**
Удаление неиспользуемых заголовков сокращает время компиляции, уменьшает количество зависимостей и повышает читаемость. Особенно важно в больших проектах.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 43: Использование `while` вместо `for` с бессмысленным счётчиком**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    for (;;) {
        printf("Running...\n");
        break;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    while (1) {
        printf("Running...\n");
        break;
    }
    return 0;
}
```

**Описание:**
`for(;;)` технически корректен, но менее читаем, чем `while(1)` в случае бесконечного цикла. Второй вариант явно указывает намерение.

**Оценка:**

* До: 5/10
* После: 8/10

---

## 🔧 **Пример 44: Вынесение повторяющейся строки в переменную**

**До рефакторинга:**

```c
#include <stdio.h>

int main() {
    printf("Error: File not found\n");
    printf("Error: File not found\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main() {
    const char* error_msg = "Error: File not found\n";
    printf("%s", error_msg);
    printf("%s", error_msg);
    return 0;
}
```

**Описание:**
Избавление от повторения одинаковой строки упрощает редактирование текста, улучшает читаемость и предотвращает дублирование ошибок.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 45: Удаление бесполезного `else` после `return`**

**До рефакторинга:**

```c
#include <stdio.h>

int check(int x) {
    if (x > 0) {
        return 1;
    } else {
        return 0;
    }
}

int main() {
    printf("%d\n", check(5));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int check(int x) {
    if (x > 0)
        return 1;
    return 0;
}

int main() {
    printf("%d\n", check(5));
    return 0;
}
```

**Описание:**
`else` после `return` не нужен — код после `return` всё равно не выполняется. Это упрощает структуру функции и улучшает читаемость.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 46: Добавление `void` в сигнатуру `main`**

**До рефакторинга:**

```c
int main() {
    return 0;
}
```

**После рефакторинга:**

```c
int main(void) {
    return 0;
}
```

**Описание:**
`int main(void)` говорит компилятору, что `main` не принимает аргументов. Это точнее и предотвращает вызовы с неожиданными параметрами. Хороший стиль для C.

**Оценка:**

* До: 8/10
* После: 10/10

---

## 🔧 **Пример 47: Удаление закомментированного кода**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    // int x = 5;
    printf("Hello\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Hello\n");
    return 0;
}
```

**Описание:**
Закомментированный код — "мёртвый" багаж. Если он больше не нужен — удалите. История кода и так сохраняется в системе контроля версий (например, Git).

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 48: Использование `exit()` с кодом ошибки**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE* f = fopen("data.txt", "r");
    if (!f) {
        printf("Error\n");
        return 1;
    }
    fclose(f);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    FILE* f = fopen("data.txt", "r");
    if (!f) {
        fprintf(stderr, "Error\n");
        exit(EXIT_FAILURE);
    }
    fclose(f);
    return 0;
}
```

**Описание:**
`exit()` даёт больше контроля (например, при работе с `atexit()`), а `EXIT_FAILURE` — читаемая альтернатива "магической единице".

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 49: Проверка результата `fopen` перед использованием**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE* f = fopen("file.txt", "r");
    char buf[100];
    fread(buf, 1, sizeof(buf), f);
    fclose(f);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE* f = fopen("file.txt", "r");
    if (!f) {
        perror("fopen failed");
        return 1;
    }
    char buf[100];
    fread(buf, 1, sizeof(buf), f);
    fclose(f);
    return 0;
}
```

**Описание:**
Проверка результата `fopen` обязательна: без неё попытка чтения из `NULL` приведёт к крэшу. Также добавлено информативное сообщение об ошибке.

**Оценка:**

* До: 3/10
* После: 10/10

---

## 🔧 **Пример 50: Использование `fprintf(stderr, …)` для сообщений об ошибках**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Error: cannot open file\n");
    return 1;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    fprintf(stderr, "Error: cannot open file\n");
    return 1;
}
```

**Описание:**
Вывод ошибок в `stderr`, а не `stdout` позволяет отделить диагностические сообщения от основного вывода. Это особенно важно при перенаправлении потоков.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 51: Использование функции для повторяющегося кода**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Error: File not found\n");
    printf("Error: File not found\n");
    printf("Error: File not found\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_error(void) {
    printf("Error: File not found\n");
}

int main(void) {
    print_error();
    print_error();
    print_error();
    return 0;
}
```

**Описание:**
Выделение повторяющегося кода в отдельную функцию уменьшает дублирование, облегчает поддержку и улучшает читаемость. При изменении сообщения достаточно поправить только функцию.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 52: Избегание глобальных переменных**

**До рефакторинга:**

```c
#include <stdio.h>

int counter = 0;

int main(void) {
    counter++;
    printf("Counter: %d\n", counter);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int counter = 0;
    counter++;
    printf("Counter: %d\n", counter);
    return 0;
}
```

**Описание:**
Минимизация использования глобальных переменных предотвращает побочные эффекты и облегчает тестирование. Локальные переменные более безопасны и предсказуемы.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 53: Использование массива вместо множества отдельных переменных**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 1, b = 2, c = 3;
    printf("%d %d %d\n", a, b, c);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[3] = {1, 2, 3};
    for (int i = 0; i < 3; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    return 0;
}
```

**Описание:**
Массивы упрощают работу с однородными данными, позволяют эффективно обрабатывать их циклом, уменьшают дублирование кода.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 54: Явное сравнение с `NULL`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char* ptr = NULL;
    if (ptr) {
        printf("Pointer is valid\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char* ptr = NULL;
    if (ptr != NULL) {
        printf("Pointer is valid\n");
    }
    return 0;
}
```

**Описание:**
Явное сравнение с `NULL` улучшает читаемость и явным образом выражает намерение программиста. Это полезно для поддерживаемого и понятного кода.

**Оценка:**

* До: 7/10
* После: 8/10

---

## 🔧 **Пример 55: Использование `const` для неизменяемых параметров**

**До рефакторинга:**

```c
#include <stdio.h>

void print_message(char* msg) {
    printf("%s\n", msg);
}

int main(void) {
    char message[] = "Hello";
    print_message(message);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_message(const char* msg) {
    printf("%s\n", msg);
}

int main(void) {
    const char message[] = "Hello";
    print_message(message);
    return 0;
}
```

**Описание:**
Объявление параметров как `const` защищает их от случайных изменений и помогает компилятору оптимизировать код, а также улучшает интерфейс функций.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 56: Использование `enum` вместо числовых констант**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int status = 1;
    if (status == 1) {
        printf("Success\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

enum Status { SUCCESS = 1, FAILURE = 0 };

int main(void) {
    enum Status status = SUCCESS;
    if (status == SUCCESS) {
        printf("Success\n");
    }
    return 0;
}
```

**Описание:**
Использование `enum` улучшает читаемость и облегчает сопровождение кода, делая смысл чисел явным.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 57: Удаление неиспользуемого параметра**

**До рефакторинга:**

```c
#include <stdio.h>

void func(int a, int b) {
    printf("%d\n", a);
}

int main(void) {
    func(1, 2);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void func(int a) {
    printf("%d\n", a);
}

int main(void) {
    func(1);
    return 0;
}
```

**Описание:**
Удаление неиспользуемых параметров упрощает интерфейс функции и снижает вероятность ошибок.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 58: Явное преобразование типов**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    double d = 3.14;
    int i = d;
    printf("%d\n", i);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    double d = 3.14;
    int i = (int)d;
    printf("%d\n", i);
    return 0;
}
```

**Описание:**
Явное приведение типов делает намерения программиста ясными и помогает избежать предупреждений компилятора.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 59: Использование `size_t` для индексов и размеров**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[10];
    int i;
    for (i = 0; i < 10; i++) {
        arr[i] = i;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[10];
    size_t i;
    for (i = 0; i < 10; i++) {
        arr[i] = (int)i;
    }
    return 0;
}
```

**Описание:**
`size_t` — стандартный тип для выражения размеров и индексов. Он беззнаковый и обеспечивает правильное поведение при больших значениях.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 60: Использование `do-while` для минимального цикла**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 0;
    while (x < 1) {
        printf("Hello\n");
        x++;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 0;
    do {
        printf("Hello\n");
        x++;
    } while (x < 1);
    return 0;
}
```

**Описание:**
Если тело цикла должно выполниться минимум один раз, `do-while` лучше выражает это намерение, делая код более очевидным.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 61: Инициализация переменных при объявлении**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x;
    x = 5;
    printf("%d\n", x);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 5;
    printf("%d\n", x);
    return 0;
}
```

**Описание:**
Инициализация переменной в момент объявления уменьшает вероятность забыть присвоить значение и повышает читаемость кода.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 62: Использование `break` для выхода из цикла вместо флага**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int found = 0;
    for (int i = 0; i < 10; i++) {
        if (i == 5) {
            found = 1;
        }
    }
    if (found) printf("Found\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    for (int i = 0; i < 10; i++) {
        if (i == 5) {
            printf("Found\n");
            break;
        }
    }
    return 0;
}
```

**Описание:**
`break` позволяет выйти из цикла сразу, что уменьшает сложность и избавляет от необходимости дополнительной переменной.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 63: Использование тернарного оператора для упрощения `if-else`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 10;
    int y;
    if (x > 5) {
        y = 1;
    } else {
        y = 0;
    }
    printf("%d\n", y);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 10;
    int y = (x > 5) ? 1 : 0;
    printf("%d\n", y);
    return 0;
}
```

**Описание:**
Тернарный оператор сокращает код и повышает компактность, сохраняя читаемость при простых условиях.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 64: Использование `switch` вместо множественных `if-else`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int code = 2;
    if (code == 1) {
        printf("One\n");
    } else if (code == 2) {
        printf("Two\n");
    } else if (code == 3) {
        printf("Three\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int code = 2;
    switch (code) {
        case 1:
            printf("One\n");
            break;
        case 2:
            printf("Two\n");
            break;
        case 3:
            printf("Three\n");
            break;
    }
    return 0;
}
```

**Описание:**
`switch` улучшает читабельность при большом числе условий и помогает компилятору оптимизировать ветвления.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 65: Использование `memset` для инициализации массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[10];
    for (int i = 0; i < 10; i++) {
        arr[i] = 0;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    int arr[10];
    memset(arr, 0, sizeof(arr));
    return 0;
}
```

**Описание:**
`memset` выполняет быстрое и эффективное заполнение памяти, упрощая код и улучшая читаемость.

**Оценка:**

* До: 5/10
* После: 8/10

---

## 🔧 **Пример 66: Использование указателя для обхода массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5] = {1, 2, 3, 4, 5};
    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5] = {1, 2, 3, 4, 5};
    int* ptr = arr;
    for (int i = 0; i < 5; i++) {
        printf("%d ", *(ptr + i));
    }
    printf("\n");
    return 0;
}
```

**Описание:**
Использование указателей для обхода массива даёт понимание работы с памятью и гибкость в манипуляциях.

**Оценка:**

* До: 7/10
* После: 8/10

---

## 🔧 **Пример 67: Замена `goto` на структурированный код**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int error = 0;
    if (error) goto cleanup;
    printf("Processing\n");
cleanup:
    printf("Cleanup\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int error = 0;
    if (!error) {
        printf("Processing\n");
    }
    printf("Cleanup\n");
    return 0;
}
```

**Описание:**
Избегание `goto` улучшает структуру кода, повышает его поддержку и снижает вероятность ошибок.

**Оценка:**

* До: 4/10
* После: 8/10

---

## 🔧 **Пример 68: Использование `inline` функции для часто вызываемого кода**

**До рефакторинга:**

```c
#include <stdio.h>

int square(int x) {
    return x * x;
}

int main(void) {
    printf("%d\n", square(5));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

inline int square(int x) {
    return x * x;
}

int main(void) {
    printf("%d\n", square(5));
    return 0;
}
```

**Описание:**
`inline` указывает компилятору встроить функцию в место вызова, что уменьшает накладные расходы на вызов и может улучшить производительность.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 69: Использование `static` для функций с внутренним использованием**

**До рефакторинга:**

```c
#include <stdio.h>

void helper(void) {
    printf("Helper\n");
}

int main(void) {
    helper();
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

static void helper(void) {
    printf("Helper\n");
}

int main(void) {
    helper();
    return 0;
}
```

**Описание:**
`static` ограничивает видимость функции файлом, уменьшая вероятность конфликтов с внешним кодом и улучшая инкапсуляцию.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 70: Использование `typedef` для упрощения сложных типов**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    struct {
        int x;
        int y;
    } point;
    point.x = 10;
    point.y = 20;
    printf("%d %d\n", point.x, point.y);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef struct {
    int x;
    int y;
} Point;

int main(void) {
    Point point;
    point.x = 10;
    point.y = 20;
    printf("%d %d\n", point.x, point.y);
    return 0;
}
```

**Описание:**
`typedef` упрощает использование типов, особенно структур, повышая читаемость и удобство поддержки.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 71: Избегание "магических чисел" с использованием констант**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int area = 3.14 * 10 * 10;
    printf("%d\n", area);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define PI 3.14
#define RADIUS 10

int main(void) {
    double area = PI * RADIUS * RADIUS;
    printf("%.2f\n", area);
    return 0;
}
```

**Описание:**
Использование именованных констант вместо "магических чисел" улучшает читаемость, облегчает изменение значений и уменьшает вероятность ошибок.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 72: Замена вложенных условий логическим оператором**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 5, y = 10;
    if (x > 0) {
        if (y > 0) {
            printf("Both positive\n");
        }
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 5, y = 10;
    if (x > 0 && y > 0) {
        printf("Both positive\n");
    }
    return 0;
}
```

**Описание:**
Использование логического оператора `&&` упрощает структуру кода, уменьшает глубину вложенности и повышает читаемость.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 73: Избегание дублирования кода через функцию**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Error: File not found\n");
    printf("Error: File not found\n");
    printf("Error: File not found\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_error(void) {
    printf("Error: File not found\n");
}

int main(void) {
    for (int i = 0; i < 3; i++) {
        print_error();
    }
    return 0;
}
```

**Описание:**
Повторяющийся код сведён в функцию и вызывается циклом, что улучшает поддержку и уменьшает дублирование.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 74: Использование `fgets` вместо `gets`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char str[100];
    gets(str);
    printf("%s\n", str);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char str[100];
    if (fgets(str, sizeof(str), stdin)) {
        printf("%s", str);
    }
    return 0;
}
```

**Описание:**
`gets` опасна и устарела из-за отсутствия проверки размера буфера. `fgets` безопаснее, предотвращает переполнение.

**Оценка:**

* До: 2/10
* После: 10/10

---

## 🔧 **Пример 75: Проверка возвращаемого значения функции**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE* f = fopen("file.txt", "r");
    // не проверяем f
    fclose(f);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE* f = fopen("file.txt", "r");
    if (f == NULL) {
        perror("Failed to open file");
        return 1;
    }
    fclose(f);
    return 0;
}
```

**Описание:**
Проверка ошибок обязательна для надёжности программ. Игнорирование может привести к сбоям.

**Оценка:**

* До: 3/10
* После: 10/10

---

## 🔧 **Пример 76: Использование циклов для заполнения массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5];
    arr[0] = 0;
    arr[1] = 1;
    arr[2] = 2;
    arr[3] = 3;
    arr[4] = 4;
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5];
    for (int i = 0; i < 5; i++) {
        arr[i] = i;
    }
    return 0;
}
```

**Описание:**
Использование цикла сокращает код и упрощает модификацию.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 77: Использование `sizeof` для вычисления размера массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[10];
    for (int i = 0; i < 10; i++) {
        arr[i] = i;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[10];
    size_t len = sizeof(arr) / sizeof(arr[0]);
    for (size_t i = 0; i < len; i++) {
        arr[i] = (int)i;
    }
    return 0;
}
```

**Описание:**
Использование `sizeof` делает код универсальным для массива любого размера.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 78: Использование функции для повторяющейся логики**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 5;
    int b = 10;
    printf("%d\n", a + b);
    printf("%d\n", a - b);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int operate(int x, int y, char op) {
    if (op == '+') return x + y;
    if (op == '-') return x - y;
    return 0;
}

int main(void) {
    int a = 5;
    int b = 10;
    printf("%d\n", operate(a, b, '+'));
    printf("%d\n", operate(a, b, '-'));
    return 0;
}
```

**Описание:**
Выделение операций в функцию упрощает расширение и повторное использование кода.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 79: Обработка ошибок с возвратом кода**

**До рефакторинга:**

```c
#include <stdio.h>

void foo(void) {
    printf("Doing work\n");
}

int main(void) {
    foo();
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int foo(void) {
    // условно считаем, что ошибка при 0
    int success = 1;
    if (!success) {
        return -1;
    }
    printf("Doing work\n");
    return 0;
}

int main(void) {
    if (foo() != 0) {
        fprintf(stderr, "Error occurred\n");
        return 1;
    }
    return 0;
}
```

**Описание:**
Передача кода ошибки из функций улучшает диагностику и контроль ошибок.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 80: Использование макроса для простого логирования**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Start\n");
    // ... код ...
    printf("End\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define LOG(msg) printf("LOG: %s\n", msg)

int main(void) {
    LOG("Start");
    // ... код ...
    LOG("End");
    return 0;
}
```

**Описание:**
Макрос упрощает добавление логов и может быть расширен или отключен в одном месте.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 81: Использование `const` для неизменяемых данных**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 10;
    x = 20; // изменение значения
    printf("%d\n", x);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    const int x = 10;
    // x = 20; // Ошибка компиляции — значение не изменяется
    printf("%d\n", x);
    return 0;
}
```

**Описание:**
Использование `const` предотвращает случайное изменение переменных, повышая безопасность кода и облегчая понимание, что значение не должно меняться.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 82: Избавление от "магических чисел" с помощью `enum`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int status = 2;
    if (status == 1) {
        printf("OK\n");
    } else if (status == 2) {
        printf("Warning\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef enum {
    STATUS_OK = 1,
    STATUS_WARNING = 2,
} Status;

int main(void) {
    Status status = STATUS_WARNING;
    if (status == STATUS_OK) {
        printf("OK\n");
    } else if (status == STATUS_WARNING) {
        printf("Warning\n");
    }
    return 0;
}
```

**Описание:**
`enum` улучшает читаемость и поддержку кода, делая числовые константы понятными.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 83: Использование функции для вывода массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[3] = {1, 2, 3};
    printf("%d %d %d\n", arr[0], arr[1], arr[2]);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_array(int *arr, size_t len) {
    for (size_t i = 0; i < len; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}

int main(void) {
    int arr[3] = {1, 2, 3};
    print_array(arr, 3);
    return 0;
}
```

**Описание:**
Выделение повторяющейся логики в функцию облегчает поддержку и повторное использование кода.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 84: Замена ручного копирования массива `memcpy`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char src[] = "Hello";
    char dest[6];
    for (int i = 0; i < 6; i++) {
        dest[i] = src[i];
    }
    printf("%s\n", dest);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char src[] = "Hello";
    char dest[6];
    memcpy(dest, src, sizeof(src));
    printf("%s\n", dest);
    return 0;
}
```

**Описание:**
`memcpy` оптимизирована для копирования блоков памяти, упрощая код и повышая производительность.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 85: Использование `for` вместо `while` для явного счётчика**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int i = 0;
    while (i < 5) {
        printf("%d\n", i);
        i++;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    for (int i = 0; i < 5; i++) {
        printf("%d\n", i);
    }
    return 0;
}
```

**Описание:**
Цикл `for` обеспечивает компактность и чёткое управление счётчиком цикла.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 86: Использование `do-while` для гарантированного выполнения**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 0;
    while (x < 5) {
        printf("%d\n", x);
        x++;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 0;
    do {
        printf("%d\n", x);
        x++;
    } while (x < 5);
    return 0;
}
```

**Описание:**
`do-while` гарантирует выполнение тела цикла минимум один раз, что полезно при необходимости начальной обработки.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 87: Использование `static` для локальных переменных при необходимости сохранения значения**

**До рефакторинга:**

```c
#include <stdio.h>

void counter() {
    int count = 0;
    count++;
    printf("%d\n", count);
}

int main(void) {
    counter();
    counter();
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void counter() {
    static int count = 0;
    count++;
    printf("%d\n", count);
}

int main(void) {
    counter();
    counter();
    return 0;
}
```

**Описание:**
`static` позволяет сохранить значение переменной между вызовами функции.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 88: Использование `assert` для отладки**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = -1;
    if (x < 0) {
        printf("Error\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <assert.h>

int main(void) {
    int x = -1;
    assert(x >= 0);
    printf("Value is %d\n", x);
    return 0;
}
```

**Описание:**
`assert` помогает выявить ошибки на ранних этапах, останавливая программу при нарушении условий.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 89: Использование `do { ... } while (0)` для безопасных макросов**

**До рефакторинга:**

```c
#define LOG(msg) printf("LOG: %s\n", msg)

int main(void) {
    if (1)
        LOG("Test");
    else
        printf("No log\n");
    return 0;
}
```

**После рефакторинга:**

```c
#define LOG(msg) do { printf("LOG: %s\n", msg); } while(0)

int main(void) {
    if (1)
        LOG("Test");
    else
        printf("No log\n");
    return 0;
}
```

**Описание:**
Макрос, обёрнутый в `do { } while(0)`, гарантирует корректное поведение в условиях и циклах.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 90: Использование `offsetof` для работы со структурами**

**До рефакторинга:**

```c
#include <stdio.h>

struct Point {
    int x;
    int y;
};

int main(void) {
    struct Point p;
    printf("x offset: 0\n"); // вручную
    printf("y offset: 4\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stddef.h>

struct Point {
    int x;
    int y;
};

int main(void) {
    printf("x offset: %zu\n", offsetof(struct Point, x));
    printf("y offset: %zu\n", offsetof(struct Point, y));
    return 0;
}
```

**Описание:**
`offsetof` позволяет безопасно получить смещение поля структуры, избегая ошибок при ручном подсчёте.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 91: Использование `break` для выхода из цикла**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int i = 0;
    while (i < 10) {
        if (i == 5) {
            i = 10;
        } else {
            printf("%d\n", i);
            i++;
        }
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int i = 0;
    while (i < 10) {
        if (i == 5) {
            break;
        }
        printf("%d\n", i);
        i++;
    }
    return 0;
}
```

**Описание:**
Использование `break` улучшает читаемость и явно показывает точку выхода из цикла, что упрощает логику.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 92: Использование `switch` вместо длинного `if-else`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int code = 2;
    if (code == 1) {
        printf("One\n");
    } else if (code == 2) {
        printf("Two\n");
    } else if (code == 3) {
        printf("Three\n");
    } else {
        printf("Other\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int code = 2;
    switch (code) {
        case 1:
            printf("One\n");
            break;
        case 2:
            printf("Two\n");
            break;
        case 3:
            printf("Three\n");
            break;
        default:
            printf("Other\n");
            break;
    }
    return 0;
}
```

**Описание:**
`switch` более структурирован и удобен для выбора между множеством вариантов, улучшая читаемость.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 93: Использование функции для подсчёта суммы массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[3] = {1, 2, 3};
    int sum = 0;
    sum += arr[0];
    sum += arr[1];
    sum += arr[2];
    printf("Sum: %d\n", sum);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int sum_array(int *arr, size_t len) {
    int sum = 0;
    for (size_t i = 0; i < len; i++) {
        sum += arr[i];
    }
    return sum;
}

int main(void) {
    int arr[3] = {1, 2, 3};
    printf("Sum: %d\n", sum_array(arr, 3));
    return 0;
}
```

**Описание:**
Вынос логики в функцию повышает повторное использование и читабельность.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 94: Использование `typedef` для упрощения записи типов**

**До рефакторинга:**

```c
#include <stdio.h>

struct Point {
    int x;
    int y;
};

int main(void) {
    struct Point p;
    p.x = 1;
    p.y = 2;
    printf("(%d, %d)\n", p.x, p.y);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef struct {
    int x;
    int y;
} Point;

int main(void) {
    Point p;
    p.x = 1;
    p.y = 2;
    printf("(%d, %d)\n", p.x, p.y);
    return 0;
}
```

**Описание:**
`typedef` позволяет упростить синтаксис, делая код более удобочитаемым.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 95: Использование функции для проверки чётности**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 4;
    if (x % 2 == 0) {
        printf("Even\n");
    } else {
        printf("Odd\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int is_even(int x) {
    return x % 2 == 0;
}

int main(void) {
    int x = 4;
    if (is_even(x)) {
        printf("Even\n");
    } else {
        printf("Odd\n");
    }
    return 0;
}
```

**Описание:**
Вынесение проверки в функцию упрощает повторное использование и повышает читаемость.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 96: Использование массива строк для упрощения выбора**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int code = 1;
    if (code == 0) printf("Zero\n");
    else if (code == 1) printf("One\n");
    else if (code == 2) printf("Two\n");
    else printf("Other\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    const char *names[] = {"Zero", "One", "Two"};
    int code = 1;
    if (code >= 0 && code < 3) {
        printf("%s\n", names[code]);
    } else {
        printf("Other\n");
    }
    return 0;
}
```

**Описание:**
Использование массива строк устраняет длинные условные конструкции и упрощает поддержку.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 97: Использование указателя на функцию для выбора действия**

**До рефакторинга:**

```c
#include <stdio.h>

void add(int a, int b) { printf("Sum: %d\n", a + b); }
void sub(int a, int b) { printf("Diff: %d\n", a - b); }

int main(void) {
    int op = 0; // 0 - add, 1 - sub
    int a = 5, b = 3;
    if (op == 0) add(a, b);
    else sub(a, b);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void add(int a, int b) { printf("Sum: %d\n", a + b); }
void sub(int a, int b) { printf("Diff: %d\n", a - b); }

int main(void) {
    void (*operations[])(int, int) = {add, sub};
    int op = 0; // 0 - add, 1 - sub
    int a = 5, b = 3;
    if (op >= 0 && op < 2) {
        operations[op](a, b);
    }
    return 0;
}
```

**Описание:**
Указатели на функции упрощают расширение и организацию кода.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 98: Использование `snprintf` вместо `sprintf`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char buf[10];
    sprintf(buf, "Number: %d", 12345);
    printf("%s\n", buf);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char buf[10];
    snprintf(buf, sizeof(buf), "Number: %d", 12345);
    printf("%s\n", buf);
    return 0;
}
```

**Описание:**
`snprintf` предотвращает переполнение буфера, повышая безопасность.

**Оценка:**

* До: 4/10
* После: 10/10

---

## 🔧 **Пример 99: Использование `calloc` для выделения и инициализации памяти**

**До рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *arr = malloc(5 * sizeof(int));
    for (int i = 0; i < 5; i++) arr[i] = 0;
    free(arr);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *arr = calloc(5, sizeof(int));
    // память уже инициализирована нулями
    free(arr);
    return 0;
}
```

**Описание:**
`calloc` объединяет выделение и обнуление памяти, упрощая код.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 100: Использование `inline` функции для оптимизации**

**До рефакторинга:**

```c
#include <stdio.h>

int square(int x) {
    return x * x;
}

int main(void) {
    printf("%d\n", square(5));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

inline int square(int x) {
    return x * x;
}

int main(void) {
    printf("%d\n", square(5));
    return 0;
}
```

**Описание:**
`inline` позволяет компилятору вставлять код функции напрямую, уменьшая накладные расходы на вызов.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 101: Упрощение условия с помощью логического оператора**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 5, b = 10;
    if (a > 0) {
        if (b > 0) {
            printf("Both positive\n");
        }
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 5, b = 10;
    if (a > 0 && b > 0) {
        printf("Both positive\n");
    }
    return 0;
}
```

**Описание:**
Объединение вложенных условий в одно с помощью логического оператора улучшает читаемость и уменьшает вложенность.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 102: Использование `enum` с явным указанием значений**

**До рефакторинга:**

```c
#include <stdio.h>

typedef enum {
    RED,
    GREEN,
    BLUE
} Color;

int main(void) {
    Color c = GREEN;
    printf("%d\n", c);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef enum {
    RED = 1,
    GREEN = 2,
    BLUE = 4
} Color;

int main(void) {
    Color c = GREEN;
    printf("%d\n", c);
    return 0;
}
```

**Описание:**
Явное указание значений в `enum` помогает, когда требуется использовать битовые флаги или конкретные значения.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 103: Использование функции для чтения числа с проверкой**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x;
    printf("Enter number: ");
    scanf("%d", &x);
    printf("You entered %d\n", x);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int read_int(void) {
    int x;
    if (scanf("%d", &x) != 1) {
        fprintf(stderr, "Invalid input\n");
        return -1;
    }
    return x;
}

int main(void) {
    printf("Enter number: ");
    int x = read_int();
    if (x != -1) {
        printf("You entered %d\n", x);
    }
    return 0;
}
```

**Описание:**
Вынесение чтения в функцию с проверкой повышает надёжность и повторное использование.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 104: Избавление от дублирования кода при инициализации**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 0;
    int b = 0;
    int c = 0;
    printf("%d %d %d\n", a, b, c);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 0, b = 0, c = 0;
    printf("%d %d %d\n", a, b, c);
    return 0;
}
```

**Описание:**
Объединение инициализаций сокращает код и делает его компактнее.

**Оценка:**

* До: 5/10
* После: 7/10

---

## 🔧 **Пример 105: Использование массива структур вместо параллельных массивов**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int ids[3] = {1, 2, 3};
    char *names[3] = {"Alice", "Bob", "Charlie"};
    for (int i = 0; i < 3; i++) {
        printf("%d: %s\n", ids[i], names[i]);
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef struct {
    int id;
    const char *name;
} Person;

int main(void) {
    Person people[3] = {
        {1, "Alice"},
        {2, "Bob"},
        {3, "Charlie"}
    };
    for (int i = 0; i < 3; i++) {
        printf("%d: %s\n", people[i].id, people[i].name);
    }
    return 0;
}
```

**Описание:**
Использование массива структур объединяет связанные данные и улучшает управление ими.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 106: Использование `enum` для статуса выполнения функции**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int success = 1;
    if (success) {
        printf("Success\n");
    } else {
        printf("Failure\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef enum {
    FAILURE = 0,
    SUCCESS = 1
} Status;

int main(void) {
    Status status = SUCCESS;
    if (status == SUCCESS) {
        printf("Success\n");
    } else {
        printf("Failure\n");
    }
    return 0;
}
```

**Описание:**
`enum` делает код более выразительным и облегчает поддержку.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 107: Использование функции для безопасного освобождения памяти**

**До рефакторинга:**

```c
#include <stdlib.h>

int main(void) {
    int *ptr = malloc(sizeof(int));
    free(ptr);
    ptr = NULL;
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdlib.h>

void safe_free(void **ptr) {
    if (ptr && *ptr) {
        free(*ptr);
        *ptr = NULL;
    }
}

int main(void) {
    int *ptr = malloc(sizeof(int));
    safe_free((void**)&ptr);
    return 0;
}
```

**Описание:**
Функция предотвращает двойное освобождение и обнуляет указатель.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 108: Использование `typedef` для указателей**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int *p;
    int a = 5;
    p = &a;
    printf("%d\n", *p);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef int* IntPtr;

int main(void) {
    IntPtr p;
    int a = 5;
    p = &a;
    printf("%d\n", *p);
    return 0;
}
```

**Описание:**
`typedef` для указателей повышает читаемость и снижает вероятность ошибок.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 109: Использование `for` для инициализации массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5];
    arr[0] = 0;
    arr[1] = 0;
    arr[2] = 0;
    arr[3] = 0;
    arr[4] = 0;
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5];
    for (int i = 0; i < 5; i++) {
        arr[i] = 0;
    }
    return 0;
}
```

**Описание:**
Использование цикла сокращает и обобщает инициализацию.

**Оценка:**

* До: 5/10
* После: 8/10

---

## 🔧 **Пример 110: Использование макроса для вычисления максимума**

**До рефакторинга:**

```c
#include <stdio.h>

int max(int a, int b) {
    return (a > b) ? a : b;
}

int main(void) {
    printf("%d\n", max(3, 5));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define MAX(a, b) ((a) > (b) ? (a) : (b))

int main(void) {
    printf("%d\n", MAX(3, 5));
    return 0;
}
```

**Описание:**
Макрос позволяет писать компактнее и избегать накладных расходов вызова функции.

**Оценка:**

* До: 7/10
* После: 8/10

---

## 🔧 **Пример 111: Замена `while` на `for` для счётчика**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int i = 0;
    while (i < 5) {
        printf("%d\n", i);
        i++;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    for (int i = 0; i < 5; i++) {
        printf("%d\n", i);
    }
    return 0;
}
```

**Описание:**
`for` удобнее использовать для счётчиков, так как инкремент и условие объединены в одном месте, улучшается читаемость.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 112: Использование `const` для неизменяемых данных**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char *msg = "Hello";
    printf("%s\n", msg);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    const char *msg = "Hello";
    printf("%s\n", msg);
    return 0;
}
```

**Описание:**
Добавление `const` защищает данные от случайного изменения, что повышает безопасность кода.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 113: Упрощение функции с тернарным оператором**

**До рефакторинга:**

```c
#include <stdio.h>

int max(int a, int b) {
    if (a > b)
        return a;
    else
        return b;
}

int main(void) {
    printf("%d\n", max(3, 5));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int max(int a, int b) {
    return (a > b) ? a : b;
}

int main(void) {
    printf("%d\n", max(3, 5));
    return 0;
}
```

**Описание:**
Тернарный оператор уменьшает количество строк и делает код компактнее.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 114: Замена магических чисел на константы**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int area = 3.14 * 5 * 5;
    printf("%d\n", area);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define PI 3.14

int main(void) {
    int radius = 5;
    double area = PI * radius * radius;
    printf("%.2f\n", area);
    return 0;
}
```

**Описание:**
Использование констант улучшает читаемость и упрощает изменение значений.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 115: Использование функции для печати массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[3] = {1, 2, 3};
    for (int i = 0; i < 3; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_array(int *arr, int len) {
    for (int i = 0; i < len; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}

int main(void) {
    int arr[3] = {1, 2, 3};
    print_array(arr, 3);
    return 0;
}
```

**Описание:**
Вынесение печати массива в функцию улучшает повторное использование и структуру.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 116: Замена цикла `while` на `do-while`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int i = 0;
    while (i < 1) {
        printf("Hello\n");
        i++;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int i = 0;
    do {
        printf("Hello\n");
        i++;
    } while (i < 1);
    return 0;
}
```

**Описание:**
`do-while` гарантирует, что тело цикла выполнится минимум один раз, что лучше отражает логику.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 117: Использование `memset` для обнуления массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5];
    for (int i = 0; i < 5; i++) {
        arr[i] = 0;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    int arr[5];
    memset(arr, 0, sizeof(arr));
    return 0;
}
```

**Описание:**
`memset` быстрее и компактнее для инициализации памяти.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 118: Использование функции для обмена значениями**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 1, b = 2, temp;
    temp = a;
    a = b;
    b = temp;
    printf("%d %d\n", a, b);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void swap(int *a, int *b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}

int main(void) {
    int a = 1, b = 2;
    swap(&a, &b);
    printf("%d %d\n", a, b);
    return 0;
}
```

**Описание:**
Функция обмена повышает читаемость и исключает дублирование кода.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 119: Использование `assert` для проверки предположений**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = -1;
    if (x >= 0) {
        printf("Valid\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <assert.h>

int main(void) {
    int x = -1;
    assert(x >= 0);
    printf("Valid\n");
    return 0;
}
```

**Описание:**
`assert` помогает выявлять ошибки на этапе разработки, делая код более надёжным.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 120: Использование `static` для ограниченной видимости**

**До рефакторинга:**

```c
#include <stdio.h>

int counter = 0;

int main(void) {
    counter++;
    printf("%d\n", counter);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

static int counter = 0;

int main(void) {
    counter++;
    printf("%d\n", counter);
    return 0;
}
```

**Описание:**
`static` ограничивает область видимости переменной файлом, улучшая инкапсуляцию.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 121: Замена громоздкого условия на функцию**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 10;
    if (x > 0 && x < 100 && x % 2 == 0) {
        printf("Valid number\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int is_valid_number(int x) {
    return x > 0 && x < 100 && x % 2 == 0;
}

int main(void) {
    int x = 10;
    if (is_valid_number(x)) {
        printf("Valid number\n");
    }
    return 0;
}
```

**Описание:**
Вынесение сложной проверки в отдельную функцию повышает читаемость и упрощает тестирование.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 122: Использование `switch` вместо множества `if-else`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int day = 3;
    if (day == 1)
        printf("Monday\n");
    else if (day == 2)
        printf("Tuesday\n");
    else if (day == 3)
        printf("Wednesday\n");
    else
        printf("Other day\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int day = 3;
    switch (day) {
        case 1:
            printf("Monday\n");
            break;
        case 2:
            printf("Tuesday\n");
            break;
        case 3:
            printf("Wednesday\n");
            break;
        default:
            printf("Other day\n");
    }
    return 0;
}
```

**Описание:**
`switch` делает код более структурированным и эффективным для выбора из множества вариантов.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 123: Использование `typedef` для структур**

**До рефакторинга:**

```c
#include <stdio.h>

struct Point {
    int x;
    int y;
};

int main(void) {
    struct Point p = {1, 2};
    printf("Point: %d, %d\n", p.x, p.y);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef struct {
    int x;
    int y;
} Point;

int main(void) {
    Point p = {1, 2};
    printf("Point: %d, %d\n", p.x, p.y);
    return 0;
}
```

**Описание:**
`typedef` упрощает синтаксис объявления переменных структурного типа.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 124: Обработка ошибок с помощью возврата кода**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "r");
    // не проверяется результат
    printf("File opened\n");
    fclose(f);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "r");
    if (f == NULL) {
        perror("Failed to open file");
        return 1;
    }
    printf("File opened\n");
    fclose(f);
    return 0;
}
```

**Описание:**
Обработка ошибок делает программу устойчивой и информативной.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 125: Использование константного указателя**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 10;
    int *p = &x;
    *p = 20;
    printf("%d\n", x);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 10;
    int * const p = &x;
    *p = 20;
    printf("%d\n", x);
    return 0;
}
```

**Описание:**
Константный указатель нельзя переназначить, что повышает безопасность.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 126: Использование макроса для определения размера массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[10];
    int len = 10;
    printf("Array length: %d\n", len);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define ARRAY_LEN(arr) (sizeof(arr) / sizeof((arr)[0]))

int main(void) {
    int arr[10];
    printf("Array length: %zu\n", ARRAY_LEN(arr));
    return 0;
}
```

**Описание:**
Макрос автоматически вычисляет размер массива, избегая ошибок.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 127: Использование функции для вычисления факториала рекурсивно**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int n = 5;
    int fact = 1;
    for (int i = 1; i <= n; i++) {
        fact *= i;
    }
    printf("%d\n", fact);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int factorial(int n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);
}

int main(void) {
    int n = 5;
    printf("%d\n", factorial(n));
    return 0;
}
```

**Описание:**
Рекурсия упрощает понимание и повторное использование кода.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 128: Использование функции для чтения строки**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char str[100];
    gets(str);  // небезопасно
    printf("%s\n", str);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char str[100];
    if (fgets(str, sizeof(str), stdin)) {
        printf("%s", str);
    }
    return 0;
}
```

**Описание:**
`fgets` безопаснее и предотвращает переполнение буфера.

**Оценка:**

* До: 3/10
* После: 9/10

---

## 🔧 **Пример 129: Использование `inline` функции**

**До рефакторинга:**

```c
#include <stdio.h>

int square(int x) {
    return x * x;
}

int main(void) {
    printf("%d\n", square(4));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

inline int square(int x) {
    return x * x;
}

int main(void) {
    printf("%d\n", square(4));
    return 0;
}
```

**Описание:**
`inline` может улучшить производительность, убрав накладные расходы вызова.

**Оценка:**

* До: 7/10
* После: 8/10

---

## 🔧 **Пример 130: Использование `sizeof` для определения размера структуры**

**До рефакторинга:**

```c
#include <stdio.h>

struct Data {
    int a;
    double b;
};

int main(void) {
    printf("Size: 16\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

struct Data {
    int a;
    double b;
};

int main(void) {
    printf("Size: %zu\n", sizeof(struct Data));
    return 0;
}
```

**Описание:**
Использование `sizeof` гарантирует корректный размер даже при изменении структуры.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 131: Замена ручного копирования строки на `strcpy`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char src[] = "Hello";
    char dest[10];
    int i = 0;
    while (src[i] != '\0') {
        dest[i] = src[i];
        i++;
    }
    dest[i] = '\0';
    printf("%s\n", dest);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char src[] = "Hello";
    char dest[10];
    strcpy(dest, src);
    printf("%s\n", dest);
    return 0;
}
```

**Описание:**
Использование стандартной функции `strcpy` упрощает код и снижает вероятность ошибок, связанных с неправильным копированием или отсутствием завершающего нуля.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 132: Использование `enum` для читаемости кода**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int status = 1;
    if (status == 0) {
        printf("OK\n");
    } else if (status == 1) {
        printf("Warning\n");
    } else if (status == 2) {
        printf("Error\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef enum {
    OK,
    WARNING,
    ERROR
} Status;

int main(void) {
    Status status = WARNING;
    if (status == OK) {
        printf("OK\n");
    } else if (status == WARNING) {
        printf("Warning\n");
    } else if (status == ERROR) {
        printf("Error\n");
    }
    return 0;
}
```

**Описание:**
Использование перечислений улучшает читаемость и предотвращает ошибки, связанные с «магическими числами».

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 133: Избегание повторного вычисления в цикле**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5] = {1,2,3,4,5};
    int sum = 0;
    for (int i = 0; i < sizeof(arr)/sizeof(arr[0]); i++) {
        sum += arr[i];
    }
    printf("%d\n", sum);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5] = {1,2,3,4,5};
    int sum = 0;
    int len = sizeof(arr) / sizeof(arr[0]);
    for (int i = 0; i < len; i++) {
        sum += arr[i];
    }
    printf("%d\n", sum);
    return 0;
}
```

**Описание:**
Вынесение вычисления длины массива из условия цикла повышает эффективность и читаемость.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 134: Использование функции для инициализации массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5];
    for (int i = 0; i < 5; i++) {
        arr[i] = 0;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

void init_array(int *arr, int size, int value) {
    for (int i = 0; i < size; i++) {
        arr[i] = value;
    }
}

int main(void) {
    int arr[5];
    init_array(arr, 5, 0);
    return 0;
}
```

**Описание:**
Выделение инициализации в функцию улучшает переиспользуемость и уменьшает дублирование.

**Оценка:**

* До: 5/10
* После: 8/10

---

## 🔧 **Пример 135: Использование `for` для обхода строки**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char str[] = "Hello";
    int i = 0;
    while (str[i] != '\0') {
        putchar(str[i]);
        i++;
    }
    putchar('\n');
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char str[] = "Hello";
    for (int i = 0; str[i] != '\0'; i++) {
        putchar(str[i]);
    }
    putchar('\n');
    return 0;
}
```

**Описание:**
`for` удобнее и компактнее для прохода по строке.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 136: Использование функции для получения длины строки**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char str[] = "Hello";
    int len = 0;
    while (str[len] != '\0') {
        len++;
    }
    printf("Length: %d\n", len);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char str[] = "Hello";
    int len = strlen(str);
    printf("Length: %d\n", len);
    return 0;
}
```

**Описание:**
Использование стандартной функции сокращает и упрощает код.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 137: Использование `break` для выхода из цикла**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5] = {1, 2, 3, 4, 5};
    int found = 0;
    for (int i = 0; i < 5; i++) {
        if (arr[i] == 3) {
            found = 1;
        }
    }
    if (found) printf("Found\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5] = {1, 2, 3, 4, 5};
    int found = 0;
    for (int i = 0; i < 5; i++) {
        if (arr[i] == 3) {
            found = 1;
            break;
        }
    }
    if (found) printf("Found\n");
    return 0;
}
```

**Описание:**
Добавление `break` предотвращает ненужные итерации после нахождения искомого элемента.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 138: Использование `fgets` вместо `gets`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char str[100];
    gets(str);
    printf("%s\n", str);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char str[100];
    if (fgets(str, sizeof(str), stdin)) {
        printf("%s", str);
    }
    return 0;
}
```

**Описание:**
`gets` опасна и удалена из стандарта, `fgets` безопаснее.

**Оценка:**

* До: 3/10
* После: 9/10

---

## 🔧 **Пример 139: Использование `calloc` для инициализации памяти**

**До рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *arr = malloc(5 * sizeof(int));
    for (int i = 0; i < 5; i++) {
        arr[i] = 0;
    }
    free(arr);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *arr = calloc(5, sizeof(int));
    free(arr);
    return 0;
}
```

**Описание:**
`calloc` выделяет и инициализирует память нулями в одном вызове.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 140: Использование функции для вычисления среднего**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[4] = {1, 2, 3, 4};
    int sum = 0;
    for (int i = 0; i < 4; i++) {
        sum += arr[i];
    }
    double avg = (double)sum / 4;
    printf("%.2f\n", avg);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

double average(int *arr, int size) {
    int sum = 0;
    for (int i = 0; i < size; i++) {
        sum += arr[i];
    }
    return (double)sum / size;
}

int main(void) {
    int arr[4] = {1, 2, 3, 4};
    printf("%.2f\n", average(arr, 4));
    return 0;
}
```

**Описание:**
Выделение вычисления среднего в функцию улучшает читаемость и повторное использование.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 141: Упрощение условия с помощью переменной**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 5, b = 10;
    if (a > 0 && b > 0 && a < 100 && b < 100) {
        printf("Both numbers are valid\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int is_valid(int x) {
    return x > 0 && x < 100;
}

int main(void) {
    int a = 5, b = 10;
    if (is_valid(a) && is_valid(b)) {
        printf("Both numbers are valid\n");
    }
    return 0;
}
```

**Описание:**
Вынесение логики проверки в функцию делает код более читабельным и облегчает поддержку.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 142: Использование массива для сопоставления символов**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char c = 'a';
    if (c == 'a') printf("Apple\n");
    else if (c == 'b') printf("Banana\n");
    else if (c == 'c') printf("Cherry\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char c = 'a';
    const char *fruits[3] = {"Apple", "Banana", "Cherry"};
    if (c >= 'a' && c <= 'c') {
        printf("%s\n", fruits[c - 'a']);
    }
    return 0;
}
```

**Описание:**
Использование массива для сопоставления символов сокращает код и улучшает расширяемость.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 143: Избавление от дублирования кода**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 5;
    if (x > 0) {
        printf("Positive: %d\n", x);
    } else {
        printf("Non-positive: %d\n", x);
    }
    if (x % 2 == 0) {
        printf("Even\n");
    } else {
        printf("Odd\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_sign(int x) {
    printf("%s: %d\n", (x > 0) ? "Positive" : "Non-positive", x);
}

void print_parity(int x) {
    printf("%s\n", (x % 2 == 0) ? "Even" : "Odd");
}

int main(void) {
    int x = 5;
    print_sign(x);
    print_parity(x);
    return 0;
}
```

**Описание:**
Выделение печати в функции повышает модульность и повторное использование.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 144: Использование `const` для неизменяемых данных**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char *msg = "Hello";
    printf("%s\n", msg);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    const char *msg = "Hello";
    printf("%s\n", msg);
    return 0;
}
```

**Описание:**
Добавление `const` повышает безопасность, запрещая изменение строкового литерала.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 145: Использование цикла `for` для заполнения массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5];
    arr[0] = 1;
    arr[1] = 2;
    arr[2] = 3;
    arr[3] = 4;
    arr[4] = 5;
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5];
    for (int i = 0; i < 5; i++) {
        arr[i] = i + 1;
    }
    return 0;
}
```

**Описание:**
Использование цикла сокращает и упрощает код.

**Оценка:**

* До: 5/10
* После: 8/10

---

## 🔧 **Пример 146: Использование `memset` для очистки массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char buffer[100];
    for (int i = 0; i < 100; i++) {
        buffer[i] = 0;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char buffer[100];
    memset(buffer, 0, sizeof(buffer));
    return 0;
}
```

**Описание:**
`memset` — стандартный и эффективный способ инициализации памяти.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 147: Использование функции для проверки четности**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 4;
    if (x % 2 == 0) {
        printf("Even\n");
    } else {
        printf("Odd\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int is_even(int x) {
    return (x % 2) == 0;
}

int main(void) {
    int x = 4;
    if (is_even(x)) {
        printf("Even\n");
    } else {
        printf("Odd\n");
    }
    return 0;
}
```

**Описание:**
Выделение проверки в функцию улучшает повторное использование.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 148: Использование функции для обмена значениями**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 5, b = 10;
    int temp = a;
    a = b;
    b = temp;
    printf("%d %d\n", a, b);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void swap(int *a, int *b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}

int main(void) {
    int a = 5, b = 10;
    swap(&a, &b);
    printf("%d %d\n", a, b);
    return 0;
}
```

**Описание:**
Выделение операции обмена в функцию повышает читаемость и повторное использование.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 149: Использование `sizeof` при выделении памяти**

**До рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *arr = malloc(5 * 4);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *arr = malloc(5 * sizeof(int));
    return 0;
}
```

**Описание:**
Использование `sizeof` повышает переносимость кода.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 150: Использование функции для сравнения строк**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char a[] = "abc";
    char b[] = "abc";
    int equal = 1;
    for (int i = 0; a[i] != '\0' && b[i] != '\0'; i++) {
        if (a[i] != b[i]) {
            equal = 0;
            break;
        }
    }
    if (equal) {
        printf("Equal\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char a[] = "abc";
    char b[] = "abc";
    if (strcmp(a, b) == 0) {
        printf("Equal\n");
    }
    return 0;
}
```

**Описание:**
Стандартная функция `strcmp` делает код проще и надежнее.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 151: Избегание глобальных переменных**

**До рефакторинга:**

```c
#include <stdio.h>

int counter = 0;

int main(void) {
    counter++;
    printf("Counter: %d\n", counter);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void increment_counter(int *counter) {
    (*counter)++;
}

int main(void) {
    int counter = 0;
    increment_counter(&counter);
    printf("Counter: %d\n", counter);
    return 0;
}
```

**Описание:**
Удаление глобальных переменных снижает зависимость и повышает модульность, что улучшает тестируемость и безопасность.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 152: Использование функции для вычисления факториала**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int n = 5;
    int result = 1;
    for (int i = 1; i <= n; i++) {
        result *= i;
    }
    printf("%d\n", result);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int factorial(int n) {
    int result = 1;
    for (int i = 1; i <= n; i++) {
        result *= i;
    }
    return result;
}

int main(void) {
    int n = 5;
    printf("%d\n", factorial(n));
    return 0;
}
```

**Описание:**
Выделение логики вычисления в функцию улучшает читаемость и позволяет переиспользовать код.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 153: Использование `switch` вместо множества `if`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char grade = 'B';
    if (grade == 'A') printf("Excellent\n");
    else if (grade == 'B') printf("Good\n");
    else if (grade == 'C') printf("Fair\n");
    else printf("Poor\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char grade = 'B';
    switch (grade) {
        case 'A':
            printf("Excellent\n");
            break;
        case 'B':
            printf("Good\n");
            break;
        case 'C':
            printf("Fair\n");
            break;
        default:
            printf("Poor\n");
            break;
    }
    return 0;
}
```

**Описание:**
`switch` улучшает читаемость и упрощает добавление новых вариантов.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 154: Устранение дублирования при вычислении среднего**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 4, b = 6, c = 8;
    double avg = (a + b + c) / 3.0;
    printf("%.2f\n", avg);
    double avg2 = (a + b + c) / 3.0;
    printf("%.2f\n", avg2);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

double average(int *arr, int size) {
    int sum = 0;
    for (int i = 0; i < size; i++) sum += arr[i];
    return (double)sum / size;
}

int main(void) {
    int nums[] = {4, 6, 8};
    double avg = average(nums, 3);
    printf("%.2f\n", avg);
    printf("%.2f\n", avg);
    return 0;
}
```

**Описание:**
Выделение вычисления среднего в функцию уменьшает дублирование и облегчает поддержку.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 155: Использование `bool` из `<stdbool.h>`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int flag = 1;
    if (flag) {
        printf("Flag is true\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdbool.h>

int main(void) {
    bool flag = true;
    if (flag) {
        printf("Flag is true\n");
    }
    return 0;
}
```

**Описание:**
Использование `bool` повышает читаемость и семантику кода.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 156: Использование `static` для локальных функций**

**До рефакторинга:**

```c
#include <stdio.h>

void helper() {
    printf("Helper\n");
}

int main(void) {
    helper();
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

static void helper() {
    printf("Helper\n");
}

int main(void) {
    helper();
    return 0;
}
```

**Описание:**
Добавление `static` ограничивает видимость функции внутри файла, улучшая инкапсуляцию.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 157: Использование `typedef` для указателей**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int *p;
    int x = 10;
    p = &x;
    printf("%d\n", *p);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef int* IntPtr;

int main(void) {
    IntPtr p;
    int x = 10;
    p = &x;
    printf("%d\n", *p);
    return 0;
}
```

**Описание:**
`typedef` улучшает читаемость, особенно для сложных типов.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 158: Использование цикла для суммирования с защитой от переполнения**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[] = {1000000, 2000000, 3000000};
    int sum = 0;
    for (int i = 0; i < 3; i++) {
        sum += arr[i];
    }
    printf("%d\n", sum);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <limits.h>
#include <stdbool.h>

bool safe_add(int a, int b, int *result) {
    if ((b > 0) && (a > INT_MAX - b)) return false;
    if ((b < 0) && (a < INT_MIN - b)) return false;
    *result = a + b;
    return true;
}

int main(void) {
    int arr[] = {1000000, 2000000, 3000000};
    int sum = 0;
    for (int i = 0; i < 3; i++) {
        if (!safe_add(sum, arr[i], &sum)) {
            printf("Overflow detected\n");
            return 1;
        }
    }
    printf("%d\n", sum);
    return 0;
}
```

**Описание:**
Добавление проверки предотвращает опасные переполнения.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 159: Использование массива структур**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char names[3][10] = {"Alice", "Bob", "Carol"};
    int ages[3] = {25, 30, 22};
    for (int i = 0; i < 3; i++) {
        printf("%s is %d years old\n", names[i], ages[i]);
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef struct {
    char name[10];
    int age;
} Person;

int main(void) {
    Person people[3] = {
        {"Alice", 25},
        {"Bob", 30},
        {"Carol", 22}
    };
    for (int i = 0; i < 3; i++) {
        printf("%s is %d years old\n", people[i].name, people[i].age);
    }
    return 0;
}
```

**Описание:**
Использование структур объединяет связанные данные, делая код более логичным.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 160: Использование функции для чтения целого числа**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x;
    scanf("%d", &x);
    printf("%d\n", x);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int read_int(void) {
    int x;
    while (scanf("%d", &x) != 1) {
        while (getchar() != '\n'); // очистка буфера
        printf("Invalid input, try again: ");
    }
    return x;
}

int main(void) {
    int x = read_int();
    printf("%d\n", x);
    return 0;
}
```

**Описание:**
Обработка ошибок ввода повышает надежность программы.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 161: Использование `enum` вместо числовых констант**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int status = 1;
    if (status == 0) printf("OK\n");
    else if (status == 1) printf("Warning\n");
    else if (status == 2) printf("Error\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef enum {
    STATUS_OK,
    STATUS_WARNING,
    STATUS_ERROR
} Status;

int main(void) {
    Status status = STATUS_WARNING;
    switch (status) {
        case STATUS_OK: printf("OK\n"); break;
        case STATUS_WARNING: printf("Warning\n"); break;
        case STATUS_ERROR: printf("Error\n"); break;
    }
    return 0;
}
```

**Описание:**
Использование `enum` улучшает читаемость и снижает вероятность ошибок, связанных с числовыми константами.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 162: Использование функции для проверки гласной**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char c = 'a';
    if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u') {
        printf("Vowel\n");
    } else {
        printf("Consonant\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <ctype.h>

int is_vowel(char c) {
    c = tolower(c);
    return c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u';
}

int main(void) {
    char c = 'a';
    if (is_vowel(c)) {
        printf("Vowel\n");
    } else {
        printf("Consonant\n");
    }
    return 0;
}
```

**Описание:**
Выделение проверки гласной в функцию повышает повторное использование и читабельность.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 163: Использование `calloc` для инициализации массива**

**До рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *arr = malloc(5 * sizeof(int));
    for (int i = 0; i < 5; i++) arr[i] = 0;
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *arr = calloc(5, sizeof(int));
    // arr уже инициализирован нулями
    return 0;
}
```

**Описание:**
`calloc` автоматически обнуляет выделенную память, что упрощает код и снижает вероятность ошибок.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 164: Использование `fprintf` для вывода ошибок**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int error = 1;
    if (error) {
        printf("Error occurred\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int error = 1;
    if (error) {
        fprintf(stderr, "Error occurred\n");
    }
    return 0;
}
```

**Описание:**
Использование `stderr` для вывода ошибок помогает разделить нормальный вывод и сообщения об ошибках.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 165: Использование макроса для вычисления максимума**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 5, b = 10;
    int max;
    if (a > b) max = a;
    else max = b;
    printf("%d\n", max);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define MAX(a,b) ((a) > (b) ? (a) : (b))

int main(void) {
    int a = 5, b = 10;
    int max = MAX(a,b);
    printf("%d\n", max);
    return 0;
}
```

**Описание:**
Макрос повышает читаемость и повторное использование кода.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 166: Избегание магических чисел**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int days_in_week = 7;
    printf("Days in week: %d\n", days_in_week);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define DAYS_IN_WEEK 7

int main(void) {
    printf("Days in week: %d\n", DAYS_IN_WEEK);
    return 0;
}
```

**Описание:**
Использование именованных констант повышает ясность и упрощает изменения.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 167: Использование `inline` функции**

**До рефакторинга:**

```c
#include <stdio.h>

int square(int x) {
    return x * x;
}

int main(void) {
    printf("%d\n", square(5));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

inline int square(int x) {
    return x * x;
}

int main(void) {
    printf("%d\n", square(5));
    return 0;
}
```

**Описание:**
Ключевое слово `inline` позволяет компилятору оптимизировать вызовы функции, снижая накладные расходы.

**Оценка:**

* До: 7/10
* После: 8/10

---

## 🔧 **Пример 168: Использование `const` для параметров функции**

**До рефакторинга:**

```c
#include <stdio.h>

void print_str(char *str) {
    printf("%s\n", str);
}

int main(void) {
    char s[] = "Hello";
    print_str(s);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_str(const char *str) {
    printf("%s\n", str);
}

int main(void) {
    char s[] = "Hello";
    print_str(s);
    return 0;
}
```

**Описание:**
Добавление `const` гарантирует, что функция не изменит строку, что повышает безопасность.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 169: Использование функции для чтения строки с ограничением**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char buf[10];
    gets(buf);
    printf("%s\n", buf);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char buf[10];
    if (fgets(buf, sizeof(buf), stdin)) {
        printf("%s", buf);
    }
    return 0;
}
```

**Описание:**
`gets` опасен и устарел, `fgets` ограничивает длину и предотвращает переполнение буфера.

**Оценка:**

* До: 3/10
* После: 9/10

---

## 🔧 **Пример 170: Использование `sizeof` при копировании массива**

**До рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char src[] = "Hello";
    char dst[6];
    for (int i = 0; i < 6; i++) {
        dst[i] = src[i];
    }
    printf("%s\n", dst);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char src[] = "Hello";
    char dst[sizeof(src)];
    memcpy(dst, src, sizeof(src));
    printf("%s\n", dst);
    return 0;
}
```

**Описание:**
Использование `memcpy` и `sizeof` сокращает код и снижает вероятность ошибок.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 171: Замена явных числовых индексов на константы**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int scores[3] = {90, 85, 88};
    printf("First score: %d\n", scores[0]);
    printf("Second score: %d\n", scores[1]);
    printf("Third score: %d\n", scores[2]);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define FIRST 0
#define SECOND 1
#define THIRD 2

int main(void) {
    int scores[3] = {90, 85, 88};
    printf("First score: %d\n", scores[FIRST]);
    printf("Second score: %d\n", scores[SECOND]);
    printf("Third score: %d\n", scores[THIRD]);
    return 0;
}
```

**Описание:**
Использование именованных констант для индексов массива повышает читаемость и снижает ошибки при изменениях.

**Оценка:**

* До: 5/10
* После: 8/10

---

## 🔧 **Пример 172: Выделение обработки ошибок в отдельную функцию**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "r");
    if (!f) {
        printf("Error opening file\n");
        return 1;
    }
    // ... чтение ...
    fclose(f);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void handle_error(const char *msg) {
    fprintf(stderr, "%s\n", msg);
}

int main(void) {
    FILE *f = fopen("file.txt", "r");
    if (!f) {
        handle_error("Error opening file");
        return 1;
    }
    // ... чтение ...
    fclose(f);
    return 0;
}
```

**Описание:**
Выделение ошибок в отдельную функцию улучшает повторное использование и чистоту основного кода.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 173: Использование `assert` для проверки предположений**

**До рефакторинга:**

```c
#include <stdio.h>

int divide(int a, int b) {
    return a / b;
}

int main(void) {
    int x = 10, y = 0;
    printf("%d\n", divide(x, y));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <assert.h>

int divide(int a, int b) {
    assert(b != 0);
    return a / b;
}

int main(void) {
    int x = 10, y = 2;
    printf("%d\n", divide(x, y));
    return 0;
}
```

**Описание:**
`assert` помогает выявлять ошибки на этапе разработки, предотвращая деление на ноль.

**Оценка:**

* До: 3/10
* После: 9/10

---

## 🔧 **Пример 174: Использование функции для печати массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[] = {1, 2, 3, 4, 5};
    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_array(const int *arr, int size) {
    for (int i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}

int main(void) {
    int arr[] = {1, 2, 3, 4, 5};
    print_array(arr, 5);
    return 0;
}
```

**Описание:**
Выделение печати массива в функцию повышает повторное использование и структурированность.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 175: Использование `const` для неизменяемых данных**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char *msg = "Hello";
    printf("%s\n", msg);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    const char *msg = "Hello";
    printf("%s\n", msg);
    return 0;
}
```

**Описание:**
Добавление `const` защищает от непреднамеренного изменения строковых литералов.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 176: Использование функции для освобождения ресурсов**

**До рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *p = malloc(sizeof(int));
    if (!p) return 1;
    *p = 42;
    printf("%d\n", *p);
    free(p);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

void cleanup(int *p) {
    free(p);
    printf("Memory freed\n");
}

int main(void) {
    int *p = malloc(sizeof(int));
    if (!p) return 1;
    *p = 42;
    printf("%d\n", *p);
    cleanup(p);
    return 0;
}
```

**Описание:**
Выделение освобождения ресурсов в функцию повышает читаемость и облегчает масштабирование.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 177: Использование `enum` для флагов**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int flags = 3; // бит 0 и 1 установлены
    if (flags & 1) printf("Flag 1 set\n");
    if (flags & 2) printf("Flag 2 set\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef enum {
    FLAG_1 = 1 << 0,
    FLAG_2 = 1 << 1
} Flags;

int main(void) {
    int flags = FLAG_1 | FLAG_2;
    if (flags & FLAG_1) printf("Flag 1 set\n");
    if (flags & FLAG_2) printf("Flag 2 set\n");
    return 0;
}
```

**Описание:**
Использование `enum` для флагов повышает читаемость и снижает ошибки при работе с битами.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 178: Улучшение читаемости с помощью промежуточных переменных**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 5, y = 10;
    printf("%d\n", (x * y) + (x - y));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 5, y = 10;
    int product = x * y;
    int difference = x - y;
    int result = product + difference;
    printf("%d\n", result);
    return 0;
}
```

**Описание:**
Использование промежуточных переменных улучшает понимание логики и упрощает отладку.

**Оценка:**

* До: 5/10
* После: 8/10

---

## 🔧 **Пример 179: Устранение магических чисел через макрос**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int radius = 5;
    double area = 3.14159 * radius * radius;
    printf("Area: %.2f\n", area);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define PI 3.14159

int main(void) {
    int radius = 5;
    double area = PI * radius * radius;
    printf("Area: %.2f\n", area);
    return 0;
}
```

**Описание:**
Выделение констант через макросы упрощает изменения и повышает читаемость.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 180: Использование `do-while` для запроса ввода**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x;
    printf("Enter number: ");
    scanf("%d", &x);
    while (x < 0) {
        printf("Invalid, try again: ");
        scanf("%d", &x);
    }
    printf("You entered: %d\n", x);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x;
    do {
        printf("Enter number: ");
        scanf("%d", &x);
    } while (x < 0);
    printf("You entered: %d\n", x);
    return 0;
}
```

**Описание:**
`do-while` улучшает логику запроса ввода, уменьшая дублирование кода.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 181: Использование `typedef` для удобства чтения**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    unsigned int a = 10;
    printf("%u\n", a);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef unsigned int uint;

int main(void) {
    uint a = 10;
    printf("%u\n", a);
    return 0;
}
```

**Описание:**
Использование `typedef` сокращает и улучшает читаемость кода, особенно при сложных типах.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 182: Улучшение обработки строки с `strncpy`**

**До рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char dest[5];
    char *src = "Hello, world!";
    strcpy(dest, src);
    printf("%s\n", dest);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char dest[5];
    char *src = "Hello, world!";
    strncpy(dest, src, sizeof(dest) - 1);
    dest[sizeof(dest) - 1] = '\0';
    printf("%s\n", dest);
    return 0;
}
```

**Описание:**
`strncpy` с явным ограничением размера буфера предотвращает переполнение и повышает безопасность.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 183: Использование функции для подсчёта длины массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[] = {1, 2, 3, 4, 5};
    int size = 5;
    for (int i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define ARRAY_SIZE(arr) (sizeof(arr) / sizeof((arr)[0]))

int main(void) {
    int arr[] = {1, 2, 3, 4, 5};
    int size = ARRAY_SIZE(arr);
    for (int i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
    return 0;
}
```

**Описание:**
Макрос `ARRAY_SIZE` позволяет автоматически вычислять размер массива, что уменьшает ошибки.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 184: Использование `switch` вместо цепочки `if-else`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char c = 'a';
    if (c == 'a') printf("A\n");
    else if (c == 'b') printf("B\n");
    else if (c == 'c') printf("C\n");
    else printf("Other\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char c = 'a';
    switch (c) {
        case 'a': printf("A\n"); break;
        case 'b': printf("B\n"); break;
        case 'c': printf("C\n"); break;
        default: printf("Other\n");
    }
    return 0;
}
```

**Описание:**
`switch` улучшает структуру и производительность по сравнению с длинной цепочкой `if-else`.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 185: Избегание повторного вызова функции**

**До рефакторинга:**

```c
#include <stdio.h>

int expensive_function() {
    printf("Called\n");
    return 42;
}

int main(void) {
    if (expensive_function() > 10) {
        printf("Greater than 10\n");
    }
    if (expensive_function() > 40) {
        printf("Greater than 40\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int expensive_function() {
    printf("Called\n");
    return 42;
}

int main(void) {
    int val = expensive_function();
    if (val > 10) {
        printf("Greater than 10\n");
    }
    if (val > 40) {
        printf("Greater than 40\n");
    }
    return 0;
}
```

**Описание:**
Кэширование результата функции уменьшает количество вызовов и улучшает производительность.

**Оценка:**

* До: 5/10
* После: 8/10

---

## 🔧 **Пример 186: Использование `static` для функции, локальной для файла**

**До рефакторинга:**

```c
#include <stdio.h>

void helper() {
    printf("Helper function\n");
}

int main(void) {
    helper();
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

static void helper() {
    printf("Helper function\n");
}

int main(void) {
    helper();
    return 0;
}
```

**Описание:**
`static` ограничивает область видимости функции текущим файлом, уменьшая риск конфликтов.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 187: Использование `enum` для состояния**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int state = 0;
    if (state == 0) printf("Idle\n");
    else if (state == 1) printf("Running\n");
    else printf("Stopped\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef enum {
    STATE_IDLE,
    STATE_RUNNING,
    STATE_STOPPED
} State;

int main(void) {
    State state = STATE_IDLE;
    switch(state) {
        case STATE_IDLE: printf("Idle\n"); break;
        case STATE_RUNNING: printf("Running\n"); break;
        case STATE_STOPPED: printf("Stopped\n"); break;
    }
    return 0;
}
```

**Описание:**
Использование `enum` улучшает ясность кода и уменьшает ошибки.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 188: Устранение дублирования в цикле**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    for (int i = 0; i < 10; i++) {
        printf("%d ", i);
    }
    printf("\n");
    for (int i = 0; i < 10; i++) {
        printf("%d ", i * 2);
    }
    printf("\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_sequence(int n, int (*func)(int)) {
    for (int i = 0; i < n; i++) {
        printf("%d ", func(i));
    }
    printf("\n");
}

int identity(int x) { return x; }
int double_val(int x) { return x * 2; }

int main(void) {
    print_sequence(10, identity);
    print_sequence(10, double_val);
    return 0;
}
```

**Описание:**
Вынесение общего кода в функцию с функцией обратного вызова уменьшает дублирование.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 189: Использование `enum` для ошибок**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int err = 1;
    if (err == 0) printf("Success\n");
    else if (err == 1) printf("File not found\n");
    else if (err == 2) printf("Access denied\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef enum {
    ERR_SUCCESS,
    ERR_FILE_NOT_FOUND,
    ERR_ACCESS_DENIED
} Error;

int main(void) {
    Error err = ERR_FILE_NOT_FOUND;
    switch(err) {
        case ERR_SUCCESS: printf("Success\n"); break;
        case ERR_FILE_NOT_FOUND: printf("File not found\n"); break;
        case ERR_ACCESS_DENIED: printf("Access denied\n"); break;
    }
    return 0;
}
```

**Описание:**
Использование `enum` для ошибок улучшает читаемость и поддерживаемость.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 190: Использование `void*` для универсальной функции**

**До рефакторинга:**

```c
#include <stdio.h>

int sum_ints(int *arr, int size) {
    int s = 0;
    for (int i = 0; i < size; i++) s += arr[i];
    return s;
}

int main(void) {
    int arr[] = {1,2,3};
    printf("%d\n", sum_ints(arr, 3));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef int (*SumFunc)(void*);

int sum_ints(void *arr, int size) {
    int *a = (int*)arr;
    int s = 0;
    for (int i = 0; i < size; i++) s += a[i];
    return s;
}

int main(void) {
    int arr[] = {1,2,3};
    printf("%d\n", sum_ints(arr, 3));
    return 0;
}
```

**Описание:**
Использование `void*` и указателей на функции повышает гибкость.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 191: Замена многократных условий на таблицу переходов**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char cmd = 'a';
    if (cmd == 'a') printf("Command A\n");
    else if (cmd == 'b') printf("Command B\n");
    else if (cmd == 'c') printf("Command C\n");
    else printf("Unknown command\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef void (*CommandFunc)(void);

void cmd_a() { printf("Command A\n"); }
void cmd_b() { printf("Command B\n"); }
void cmd_c() { printf("Command C\n"); }
void cmd_unknown() { printf("Unknown command\n"); }

int main(void) {
    char cmd = 'a';
    struct {
        char key;
        CommandFunc func;
    } commands[] = {
        {'a', cmd_a},
        {'b', cmd_b},
        {'c', cmd_c}
    };

    int found = 0;
    for (int i = 0; i < sizeof(commands)/sizeof(commands[0]); i++) {
        if (commands[i].key == cmd) {
            commands[i].func();
            found = 1;
            break;
        }
    }
    if (!found) cmd_unknown();
    return 0;
}
```

**Описание:**
Таблица переходов с функциями позволяет легко расширять команды и избегать длинных цепочек условий.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 192: Улучшение чтения с помощью промежуточных функций**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 5, b = 3;
    printf("%d + %d = %d\n", a, b, a + b);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int add(int x, int y) {
    return x + y;
}

int main(void) {
    int a = 5, b = 3;
    printf("%d + %d = %d\n", a, b, add(a, b));
    return 0;
}
```

**Описание:**
Выделение операции сложения в функцию повышает переиспользуемость и ясность.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 193: Замена магических чисел константами**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int age = 20;
    if (age >= 18) printf("Adult\n");
    else printf("Minor\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define ADULT_AGE 18

int main(void) {
    int age = 20;
    if (age >= ADULT_AGE) printf("Adult\n");
    else printf("Minor\n");
    return 0;
}
```

**Описание:**
Константы облегчают понимание кода и последующие изменения.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 194: Оптимизация вложенных условий**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 10;
    if (x > 0) {
        if (x < 20) {
            printf("x is between 1 and 19\n");
        }
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 10;
    if (x > 0 && x < 20) {
        printf("x is between 1 and 19\n");
    }
    return 0;
}
```

**Описание:**
Объединение условий уменьшает вложенность и повышает читаемость.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 195: Использование функции для повторяющегося кода**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Hello, World!\n");
    printf("Hello, User!\n");
    printf("Hello, Admin!\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void greet(const char *name) {
    printf("Hello, %s!\n", name);
}

int main(void) {
    greet("World");
    greet("User");
    greet("Admin");
    return 0;
}
```

**Описание:**
Функция упрощает повторное использование и изменение логики приветствия.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 196: Замена `for` на `while` для лучшей читаемости**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    for (int i = 0; i < 10; i++) {
        printf("%d ", i);
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int i = 0;
    while (i < 10) {
        printf("%d ", i);
        i++;
    }
    return 0;
}
```

**Описание:**
Иногда `while` улучшает читаемость и контроль над циклом.

**Оценка:**

* До: 7/10
* После: 7/10 (вариант, зависит от контекста)

---

## 🔧 **Пример 197: Проверка возврата `malloc`**

**До рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *p = malloc(sizeof(int)*10);
    // нет проверки
    p[0] = 1;
    printf("%d\n", p[0]);
    free(p);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *p = malloc(sizeof(int)*10);
    if (!p) {
        fprintf(stderr, "Memory allocation failed\n");
        return 1;
    }
    p[0] = 1;
    printf("%d\n", p[0]);
    free(p);
    return 0;
}
```

**Описание:**
Проверка возвращаемого указателя после `malloc` — обязательная практика для предотвращения сбоев.

**Оценка:**

* До: 3/10
* После: 10/10

---

## 🔧 **Пример 198: Использование `memset` для инициализации**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[10];
    for (int i = 0; i < 10; i++) arr[i] = 0;
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    int arr[10];
    memset(arr, 0, sizeof(arr));
    return 0;
}
```

**Описание:**
`memset` быстрее и короче для инициализации больших блоков памяти.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 199: Выделение констант в enum**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int level = 2;
    if (level == 0) printf("Low\n");
    else if (level == 1) printf("Medium\n");
    else if (level == 2) printf("High\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef enum {
    LOW,
    MEDIUM,
    HIGH
} Level;

int main(void) {
    Level level = HIGH;
    switch(level) {
        case LOW: printf("Low\n"); break;
        case MEDIUM: printf("Medium\n"); break;
        case HIGH: printf("High\n"); break;
    }
    return 0;
}
```

**Описание:**
Enum упрощает понимание и поддержку уровня.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 200: Использование `inline` функций**

**До рефакторинга:**

```c
#include <stdio.h>

int square(int x) {
    return x * x;
}

int main(void) {
    printf("%d\n", square(5));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

inline int square(int x) {
    return x * x;
}

int main(void) {
    printf("%d\n", square(5));
    return 0;
}
```

**Описание:**
`inline` позволяет компилятору оптимизировать вызовы функции, уменьшив накладные расходы.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 201: Замена ручного подсчёта длины строки на `strlen`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char *str = "Hello";
    int len = 0;
    while (str[len] != '\0') len++;
    printf("Length: %d\n", len);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char *str = "Hello";
    int len = strlen(str);
    printf("Length: %d\n", len);
    return 0;
}
```

**Описание:**
Использование стандартной функции `strlen` улучшает читаемость и производительность за счёт оптимизации библиотек.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 202: Обработка ошибок при открытии файла**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "r");
    // Нет проверки
    char c = fgetc(f);
    printf("%c\n", c);
    fclose(f);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "r");
    if (!f) {
        perror("Failed to open file");
        return 1;
    }
    char c = fgetc(f);
    printf("%c\n", c);
    fclose(f);
    return 0;
}
```

**Описание:**
Проверка успешного открытия файла предотвращает крахи и помогает диагностировать ошибки.

**Оценка:**

* До: 4/10
* После: 10/10

---

## 🔧 **Пример 203: Использование `const` для неизменяемых данных**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char *msg = "Hello";
    printf("%s\n", msg);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    const char *msg = "Hello";
    printf("%s\n", msg);
    return 0;
}
```

**Описание:**
Использование `const` гарантирует, что строка не будет изменена, что помогает избежать ошибок и оптимизирует код.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 204: Упрощение условия с помощью тернарного оператора**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 10;
    if (a > 5) printf("Large\n");
    else printf("Small\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 10;
    printf("%s\n", (a > 5) ? "Large" : "Small");
    return 0;
}
```

**Описание:**
Тернарный оператор сокращает и делает код компактнее при простых условиях.

**Оценка:**

* До: 7/10
* После: 8/10

---

## 🔧 **Пример 205: Использование `for` для заполнения массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5];
    arr[0] = 0;
    arr[1] = 1;
    arr[2] = 2;
    arr[3] = 3;
    arr[4] = 4;
    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5];
    for (int i = 0; i < 5; i++) {
        arr[i] = i;
    }
    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }
    return 0;
}
```

**Описание:**
Циклы упрощают и уменьшают количество кода, повышая его поддержку.

**Оценка:**

* До: 5/10
* После: 8/10

---

## 🔧 **Пример 206: Выделение памяти для строки с использованием `strdup`**

**До рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {
    char *s = malloc(6);
    strcpy(s, "Hello");
    printf("%s\n", s);
    free(s);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {
    char *s = strdup("Hello");
    if (!s) {
        perror("strdup failed");
        return 1;
    }
    printf("%s\n", s);
    free(s);
    return 0;
}
```

**Описание:**
`strdup` упрощает выделение памяти и копирование строки.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 207: Использование функции для проверки чётности**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 4;
    if (x % 2 == 0) printf("Even\n");
    else printf("Odd\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int is_even(int x) {
    return (x % 2) == 0;
}

int main(void) {
    int x = 4;
    printf("%s\n", is_even(x) ? "Even" : "Odd");
    return 0;
}
```

**Описание:**
Функция улучшает читабельность и позволяет использовать логику повторно.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 208: Использование `switch` для выбора действия**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char op = '+';
    int a = 5, b = 3;
    if (op == '+') printf("%d\n", a + b);
    else if (op == '-') printf("%d\n", a - b);
    else printf("Unknown\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char op = '+';
    int a = 5, b = 3;
    switch(op) {
        case '+': printf("%d\n", a + b); break;
        case '-': printf("%d\n", a - b); break;
        default: printf("Unknown\n");
    }
    return 0;
}
```

**Описание:**
`switch` улучшает структуру и расширяемость по сравнению с цепочкой `if-else`.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 209: Использование `for` с объявлением счетчика**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int i;
    for (i = 0; i < 5; i++) {
        printf("%d\n", i);
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    for (int i = 0; i < 5; i++) {
        printf("%d\n", i);
    }
    return 0;
}
```

**Описание:**
Объявление переменной цикла внутри `for` ограничивает область видимости.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 210: Использование `do-while` для чтения ввода**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 0;
    scanf("%d", &x);
    while (x != 0) {
        printf("Input: %d\n", x);
        scanf("%d", &x);
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x;
    do {
        scanf("%d", &x);
        if (x != 0)
            printf("Input: %d\n", x);
    } while (x != 0);
    return 0;
}
```

**Описание:**
`do-while` гарантирует выполнение цикла хотя бы один раз, что логично для ввода.

**Оценка:**

* До: 7/10
* После: 8/10

---

## 🔧 **Пример 211: Использование `sizeof(*ptr)` вместо типа**

**До рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *arr = malloc(10 * sizeof(int));
    if (!arr) return 1;
    free(arr);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *arr = malloc(10 * sizeof(*arr));
    if (!arr) return 1;
    free(arr);
    return 0;
}
```

**Описание:**
Использование `sizeof(*arr)` делает код менее подверженным ошибкам при изменении типа переменной, так как тип вычисляется автоматически.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 212: Приведение ввода к функции с возвратом результата**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x;
    printf("Enter number: ");
    scanf("%d", &x);
    printf("You entered: %d\n", x);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int read_number(void) {
    int x;
    printf("Enter number: ");
    scanf("%d", &x);
    return x;
}

int main(void) {
    int x = read_number();
    printf("You entered: %d\n", x);
    return 0;
}
```

**Описание:**
Вынос логики ввода в отдельную функцию улучшает читаемость и позволяет повторно использовать код.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 213: Использование `exit` при фатальной ошибке**

**До рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    FILE *f = fopen("data.txt", "r");
    if (!f) {
        printf("File open error\n");
        return 1;
    }
    fclose(f);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    FILE *f = fopen("data.txt", "r");
    if (!f) {
        perror("File open error");
        exit(EXIT_FAILURE);
    }
    fclose(f);
    return 0;
}
```

**Описание:**
Использование `exit()` даёт более явный сигнал об аварийном завершении и не зависит от контекста возврата из `main`.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 214: Избежание повторной строки в `printf`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 5;
    printf("Value is %d\n", x);
    printf("Value is %d again\n", x);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_value(const char *msg, int x) {
    printf("%s %d\n", msg, x);
}

int main(void) {
    int x = 5;
    print_value("Value is", x);
    print_value("Value is again", x);
    return 0;
}
```

**Описание:**
Функция с параметром упрощает повторяющийся формат вывода.

**Оценка:**

* До: 5/10
* После: 8/10

---

## 🔧 **Пример 215: Использование `isdigit` вместо ручной проверки**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char c = '5';
    if (c >= '0' && c <= '9') {
        printf("Digit\n");
    } else {
        printf("Not digit\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <ctype.h>

int main(void) {
    char c = '5';
    if (isdigit((unsigned char)c)) {
        printf("Digit\n");
    } else {
        printf("Not digit\n");
    }
    return 0;
}
```

**Описание:**
Функции из `<ctype.h>` обеспечивают читаемость и корректную обработку локалей.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 216: Использование `enum` для логических состояний**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int enabled = 1;
    if (enabled) printf("Enabled\n");
    else printf("Disabled\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef enum { DISABLED, ENABLED } State;

int main(void) {
    State s = ENABLED;
    printf("%s\n", (s == ENABLED) ? "Enabled" : "Disabled");
    return 0;
}
```

**Описание:**
`enum` улучшает семантику логических значений и предотвращает путаницу с обычными числами.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 217: Выделение обработки командной строки в отдельную функцию**

**До рефакторинга:**

```c
#include <stdio.h>

int main(int argc, char *argv[]) {
    if (argc > 1) {
        printf("Argument: %s\n", argv[1]);
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void handle_args(int argc, char *argv[]) {
    if (argc > 1) {
        printf("Argument: %s\n", argv[1]);
    }
}

int main(int argc, char *argv[]) {
    handle_args(argc, argv);
    return 0;
}
```

**Описание:**
Выделение логики аргументов улучшает читаемость и модульность `main`.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 218: Проверка возвращаемого значения `scanf`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x;
    scanf("%d", &x);
    printf("%d\n", x);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x;
    if (scanf("%d", &x) != 1) {
        fprintf(stderr, "Input error\n");
        return 1;
    }
    printf("%d\n", x);
    return 0;
}
```

**Описание:**
Проверка результата `scanf` предотвращает использование неинициализированных переменных.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 219: Инициализация указателя `NULL` по умолчанию**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char *str;
    str = "test";
    printf("%s\n", str);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char *str = NULL;
    str = "test";
    printf("%s\n", str);
    return 0;
}
```

**Описание:**
Начальная инициализация `NULL` помогает в будущем проверять корректность инициализации и избегать мусора.

**Оценка:**

* До: 5/10
* После: 8/10

---

## 🔧 **Пример 220: Использование `fgets` вместо `gets`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char buffer[100];
    gets(buffer);
    printf("You entered: %s\n", buffer);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char buffer[100];
    if (fgets(buffer, sizeof(buffer), stdin)) {
        printf("You entered: %s", buffer);
    }
    return 0;
}
```

**Описание:**
Функция `gets` удалена из стандарта из-за небезопасности — `fgets` безопасна и управляет переполнением буфера.

**Оценка:**

* До: 2/10
* После: 10/10

---

## 🔧 **Пример 221: Проверка `NULL` перед `free()`**

**До рефакторинга:**

```c
#include <stdlib.h>

int main(void) {
    int *ptr = malloc(sizeof(int) * 5);
    free(ptr);
    free(ptr); // Undefined behavior
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdlib.h>

int main(void) {
    int *ptr = malloc(sizeof(int) * 5);
    free(ptr);
    ptr = NULL;
    if (ptr) {
        free(ptr);
    }
    return 0;
}
```

**Описание:**
После освобождения памяти указатель должен быть обнулён — это предотвращает двойное освобождение, что ведёт к ошибкам времени выполнения.

**Оценка:**

* До: 3/10
* После: 9/10

---

## 🔧 **Пример 222: Использование `const` в параметрах функции**

**До рефакторинга:**

```c
#include <stdio.h>

void print(const char *str) {
    printf("%s\n", str);
}

int main(void) {
    char *msg = "Hello";
    print(msg);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print(const char * const str) {
    printf("%s\n", str);
}

int main(void) {
    const char *msg = "Hello";
    print(msg);
    return 0;
}
```

**Описание:**
Объявление параметра как `const char * const` гарантирует, что ни указатель, ни содержимое не будут изменены внутри функции.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 223: Улучшение форматирования чисел**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 5;
    printf("Value: %d\n", x);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 5;
    printf("Value: %04d\n", x); // добавлено выравнивание
    return 0;
}
```

**Описание:**
Форматирование вывода (например, `%04d`) делает вывод более аккуратным и пригодным для табличных данных или логов.

**Оценка:**

* До: 7/10
* После: 8/10

---

## 🔧 **Пример 224: Вынос расчётов в отдельную функцию**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 3, b = 4;
    int area = a * b;
    printf("Area: %d\n", area);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int calc_area(int width, int height) {
    return width * height;
}

int main(void) {
    int a = 3, b = 4;
    printf("Area: %d\n", calc_area(a, b));
    return 0;
}
```

**Описание:**
Вынос простой логики в функцию повышает переиспользуемость и облегчает тестирование.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 225: Проверка границ массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5] = {1,2,3,4,5};
    for (int i = 0; i <= 5; i++) {
        printf("%d ", arr[i]); // переполнение
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5] = {1,2,3,4,5};
    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }
    return 0;
}
```

**Описание:**
Цикл с правильной границей предотвращает доступ за пределы массива, что может вызвать сбои или уязвимости.

**Оценка:**

* До: 2/10
* После: 10/10

---

## 🔧 **Пример 226: Использование `sizeof(array)/sizeof(type)`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[] = {1, 2, 3};
    for (int i = 0; i < 3; i++) {
        printf("%d\n", arr[i]);
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[] = {1, 2, 3};
    int len = sizeof(arr) / sizeof(arr[0]);
    for (int i = 0; i < len; i++) {
        printf("%d\n", arr[i]);
    }
    return 0;
}
```

**Описание:**
Расчёт длины массива автоматически предотвращает ошибки при изменении размеров массива вручную.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 227: Использование булевого типа `stdbool.h`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int flag = 1;
    if (flag) {
        printf("True\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdbool.h>

int main(void) {
    bool flag = true;
    if (flag) {
        printf("True\n");
    }
    return 0;
}
```

**Описание:**
Тип `bool` из `stdbool.h` делает код семантически понятнее и ближе к логическим выражениям.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 228: Обработка строки с `strncpy` вместо `strcpy`**

**До рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char dest[5];
    strcpy(dest, "hello");
    printf("%s\n", dest);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char dest[5];
    strncpy(dest, "hello", sizeof(dest) - 1);
    dest[4] = '\0';
    printf("%s\n", dest);
    return 0;
}
```

**Описание:**
Использование `strncpy` предотвращает переполнение буфера, делая код безопаснее.

**Оценка:**

* До: 2/10
* После: 9/10

---

## 🔧 **Пример 229: Удаление неиспользуемой переменной**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int unused = 42;
    printf("Hello\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Hello\n");
    return 0;
}
```

**Описание:**
Удаление неиспользуемых переменных избавляет от предупреждений и делает код чище.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 230: Использование тернарного оператора в return**

**До рефакторинга:**

```c
#include <stdio.h>

int is_positive(int x) {
    if (x > 0) return 1;
    else return 0;
}

int main(void) {
    printf("%d\n", is_positive(5));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int is_positive(int x) {
    return x > 0 ? 1 : 0;
}

int main(void) {
    printf("%d\n", is_positive(5));
    return 0;
}
```

**Описание:**
Сокращение логики через тернарный оператор делает код компактным и легко читаемым.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 231: Использование `fputs` вместо `fprintf` для простого вывода**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    fprintf(stdout, "Hello, world!\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    fputs("Hello, world!\n", stdout);
    return 0;
}
```

**Описание:**
Если не требуется форматирование, `fputs` предпочтительнее: он проще, быстрее и избегает лишней обработки форматной строки.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 232: Использование `strncmp` для безопасного сравнения строк**

**До рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char input[5];
    scanf("%s", input);
    if (strcmp(input, "yes") == 0) {
        printf("Confirmed\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char input[5];
    scanf("%4s", input);
    if (strncmp(input, "yes", 4) == 0) {
        printf("Confirmed\n");
    }
    return 0;
}
```

**Описание:**
`strncmp` защищает от выхода за границы буфера при сравнении строк, особенно если строка не нуль-терминирована.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 233: Использование `sizeof(arr)/sizeof(arr[0])` в функциях**

**До рефакторинга:**

```c
#include <stdio.h>

void print_array(int *arr, int size) {
    for (int i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
}

int main(void) {
    int data[] = {1, 2, 3, 4};
    print_array(data, 4);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define ARRAY_SIZE(arr) (sizeof(arr)/sizeof((arr)[0]))

void print_array(int *arr, int size) {
    for (int i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
}

int main(void) {
    int data[] = {1, 2, 3, 4};
    print_array(data, ARRAY_SIZE(data));
    return 0;
}
```

**Описание:**
Макрос `ARRAY_SIZE` уменьшает вероятность ошибки при передаче длины массива в функции.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 234: Явная инициализация переменных**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int count;
    printf("Count is %d\n", count); // неопределённое значение
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int count = 0;
    printf("Count is %d\n", count);
    return 0;
}
```

**Описание:**
Неинициализированные переменные могут вести к непредсказуемому поведению. Инициализация по умолчанию — хорошая практика.

**Оценка:**

* До: 3/10
* После: 9/10

---

## 🔧 **Пример 235: Использование `return EXIT_SUCCESS` и `EXIT_FAILURE`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Done\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    printf("Done\n");
    return EXIT_SUCCESS;
}
```

**Описание:**
Использование `EXIT_SUCCESS` и `EXIT_FAILURE` делает код более переносимым и семантически правильным.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 236: Проверка диапазона значения перед использованием**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int index = 10;
    int arr[10];
    arr[index] = 5; // переполнение
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int index = 10;
    int arr[10];
    if (index >= 0 && index < 10) {
        arr[index] = 5;
    } else {
        printf("Index out of bounds\n");
    }
    return 0;
}
```

**Описание:**
Проверка индекса перед доступом к массиву — обязательное условие безопасности и стабильности программы.

**Оценка:**

* До: 2/10
* После: 10/10

---

## 🔧 **Пример 237: Использование `const` для неизменяемых параметров массива**

**До рефакторинга:**

```c
#include <stdio.h>

void print_values(int arr[], int size) {
    for (int i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
}

int main(void) {
    int data[] = {10, 20, 30};
    print_values(data, 3);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_values(const int arr[], int size) {
    for (int i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
}

int main(void) {
    int data[] = {10, 20, 30};
    print_values(data, 3);
    return 0;
}
```

**Описание:**
Объявление массива `const` в параметрах функции сообщает компилятору и другим разработчикам, что данные не будут изменены.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 238: Именование переменных по смыслу**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 10, b = 5;
    int c = a - b;
    printf("%d\n", c);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int height = 10, depth = 5;
    int elevation = height - depth;
    printf("%d\n", elevation);
    return 0;
}
```

**Описание:**
Понятные имена переменных улучшают читаемость и поддержку кода.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 239: Избежание магических чисел**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int radius = 5;
    float area = 3.14 * radius * radius;
    printf("Area: %.2f\n", area);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define PI 3.14159265358979323846

int main(void) {
    int radius = 5;
    float area = PI * radius * radius;
    printf("Area: %.2f\n", area);
    return 0;
}
```

**Описание:**
Константы с именами вместо "магических чисел" делают код самодокументированным и позволяют легко менять значения.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 240: Инициализация структуры при объявлении**

**До рефакторинга:**

```c
#include <stdio.h>

typedef struct {
    int x;
    int y;
} Point;

int main(void) {
    Point p;
    p.x = 10;
    p.y = 20;
    printf("%d %d\n", p.x, p.y);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef struct {
    int x;
    int y;
} Point;

int main(void) {
    Point p = {10, 20};
    printf("%d %d\n", p.x, p.y);
    return 0;
}
```

**Описание:**
Инициализация при объявлении уменьшает количество строк и повышает читаемость, особенно при простых значениях.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 241: Замена `while(1)` на `for(;;)` для бесконечного цикла**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    while (1) {
        printf("Running...\n");
        break;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    for (;;) {
        printf("Running...\n");
        break;
    }
    return 0;
}
```

**Описание:**
`for(;;)` лучше отражает бесконечную природу цикла и чуть эффективнее, так как не требует вычисления логического значения.

**Оценка:**

* До: 7/10
* После: 8/10

---

## 🔧 **Пример 242: Использование `break` для выхода из цикла вместо флага**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int i = 0;
    int done = 0;
    while (!done) {
        printf("%d\n", i);
        if (i == 5) {
            done = 1;
        }
        i++;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int i = 0;
    while (1) {
        printf("%d\n", i);
        if (i == 5) break;
        i++;
    }
    return 0;
}
```

**Описание:**
Прямой выход из цикла через `break` упрощает логику и избавляет от лишнего флага.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 243: Использование `return` вместо вложенного `else`**

**До рефакторинга:**

```c
#include <stdio.h>

int is_even(int n) {
    if (n % 2 == 0) {
        return 1;
    } else {
        return 0;
    }
}

int main(void) {
    printf("%d\n", is_even(4));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int is_even(int n) {
    if (n % 2 == 0) return 1;
    return 0;
}

int main(void) {
    printf("%d\n", is_even(4));
    return 0;
}
```

**Описание:**
Избавление от ненужных `else` конструкций после `return` упрощает чтение и структуру функции.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 244: Использование `calloc` вместо `malloc` + `memset`**

**До рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {
    int *arr = malloc(10 * sizeof(int));
    memset(arr, 0, 10 * sizeof(int));
    free(arr);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *arr = calloc(10, sizeof(int));
    free(arr);
    return 0;
}
```

**Описание:**
`calloc` более читаем и безопасен для инициализации памяти нулями — избавляет от ручного вызова `memset`.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 245: Использование `feof` корректно**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "r");
    char buf[100];
    while (!feof(f)) {
        fgets(buf, sizeof(buf), f);
        printf("%s", buf);
    }
    fclose(f);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "r");
    char buf[100];
    while (fgets(buf, sizeof(buf), f)) {
        printf("%s", buf);
    }
    fclose(f);
    return 0;
}
```

**Описание:**
`feof` не срабатывает до попытки чтения, поэтому использовать `fgets` в условии — корректнее.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 246: Использование `static inline` функций в заголовках**

**До рефакторинга:**

```c
// math_utils.h
int square(int x);
```

```c
// math_utils.c
int square(int x) {
    return x * x;
}
```

**После рефакторинга:**

```c
// math_utils.h
static inline int square(int x) {
    return x * x;
}
```

**Описание:**
`static inline` позволяет встраивать функцию в код без линковки `.c` файла, повышая производительность и упрощая заголовок.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 247: Проверка успешного открытия файла**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("data.txt", "r");
    // используем f без проверки
    fclose(f);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("data.txt", "r");
    if (!f) {
        perror("fopen");
        return 1;
    }
    fclose(f);
    return 0;
}
```

**Описание:**
Без проверки `fopen` можно вызвать `fclose` на `NULL`, что приведёт к неопределённому поведению.

**Оценка:**

* До: 4/10
* После: 10/10

---

## 🔧 **Пример 248: Использование `#define` для повторяющегося значения**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int buffer[1024];
    printf("Size: %d\n", 1024);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define BUFFER_SIZE 1024

int main(void) {
    int buffer[BUFFER_SIZE];
    printf("Size: %d\n", BUFFER_SIZE);
    return 0;
}
```

**Описание:**
Использование `#define` или `const` переменной повышает читаемость и уменьшает риск несогласованности.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 249: Проверка результата `malloc` перед использованием**

**До рефакторинга:**

```c
#include <stdlib.h>
#include <string.h>

int main(void) {
    char *s = malloc(100);
    strcpy(s, "hello");
    free(s);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdlib.h>
#include <string.h>
#include <stdio.h>

int main(void) {
    char *s = malloc(100);
    if (!s) {
        fprintf(stderr, "Memory allocation failed\n");
        return 1;
    }
    strcpy(s, "hello");
    free(s);
    return 0;
}
```

**Описание:**
Отсутствие проверки после `malloc` может привести к `NULL` разыменованию и краху программы.

**Оценка:**

* До: 3/10
* После: 10/10

---

## 🔧 **Пример 250: Избежание использования `goto`**

**До рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    FILE *f = fopen("data.txt", "r");
    if (!f) goto error;

    // Работа с файлом

    fclose(f);
    return 0;

error:
    printf("Error opening file\n");
    return 1;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    FILE *f = fopen("data.txt", "r");
    if (!f) {
        printf("Error opening file\n");
        return 1;
    }

    // Работа с файлом

    fclose(f);
    return 0;
}
```

**Описание:**
`goto` ухудшает читаемость и структуру программы. Лучше использовать блоки и `return`.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 251: Удаление лишнего `else` после `return`**

**До рефакторинга:**

```c
#include <stdio.h>

int get_value(int x) {
    if (x > 0) {
        return 1;
    } else {
        return -1;
    }
}

int main(void) {
    printf("%d\n", get_value(5));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int get_value(int x) {
    if (x > 0) return 1;
    return -1;
}

int main(void) {
    printf("%d\n", get_value(5));
    return 0;
}
```

**Описание:**
Удаление `else` после `return` улучшает читаемость и уменьшает вложенность. Это особенно полезно в длинных функциях.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 252: Использование `fprintf(stderr, ...)` для ошибок**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Error: File not found\n");
    return 1;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    fprintf(stderr, "Error: File not found\n");
    return 1;
}
```

**Описание:**
Вывод ошибок в `stderr` позволяет отделить их от стандартного вывода, что важно при перенаправлении потоков.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 253: Использование `do { ... } while (0)` в макросах**

**До рефакторинга:**

```c
#define PRINT_TWO(x, y) printf("%d\n", x); printf("%d\n", y)

int main(void) {
    if (1)
        PRINT_TWO(1, 2);
    else
        printf("Nope\n");
    return 0;
}
```

**После рефакторинга:**

```c
#define PRINT_TWO(x, y) do { printf("%d\n", x); printf("%d\n", y); } while (0)

int main(void) {
    if (1)
        PRINT_TWO(1, 2);
    else
        printf("Nope\n");
    return 0;
}
```

**Описание:**
Макрос с `do { ... } while (0)` — безопасен при использовании в условных выражениях и предотвращает ошибки синтаксиса.

**Оценка:**

* До: 3/10
* После: 9/10

---

## 🔧 **Пример 254: Минимизация повторов с помощью функции**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Header\n");
    printf("Item: A\n");
    printf("Item: B\n");
    printf("Item: C\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print_item(const char *item) {
    printf("Item: %s\n", item);
}

int main(void) {
    printf("Header\n");
    print_item("A");
    print_item("B");
    print_item("C");
    return 0;
}
```

**Описание:**
Вынос повторяющегося кода в отдельную функцию делает код чище и упрощает его поддержку.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 255: Использование `const` для неизменяемых строк**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char *msg = "Welcome";
    printf("%s\n", msg);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    const char *msg = "Welcome";
    printf("%s\n", msg);
    return 0;
}
```

**Описание:**
Литералы строк должны быть объявлены как `const char *`, так как они размещаются в памяти только для чтения.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 256: Замена глобальной переменной локальной**

**До рефакторинга:**

```c
#include <stdio.h>

int counter = 0;

int main(void) {
    counter++;
    printf("%d\n", counter);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int counter = 0;
    counter++;
    printf("%d\n", counter);
    return 0;
}
```

**Описание:**
Глобальные переменные усложняют поддержку и отладку кода. Лучше использовать локальные, когда возможно.

**Оценка:**

* До: 4/10
* После: 9/10

---

## 🔧 **Пример 257: Использование `size_t` вместо `int` для размеров**

**До рефакторинга:**

```c
#include <stdio.h>

void print(int arr[], int len) {
    for (int i = 0; i < len; i++) {
        printf("%d ", arr[i]);
    }
}

int main(void) {
    int arr[] = {1,2,3};
    print(arr, 3);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print(int arr[], size_t len) {
    for (size_t i = 0; i < len; i++) {
        printf("%d ", arr[i]);
    }
}

int main(void) {
    int arr[] = {1,2,3};
    print(arr, sizeof(arr)/sizeof(arr[0]));
    return 0;
}
```

**Описание:**
`size_t` — тип, предназначенный для представления размеров. Это повышает совместимость и безопасность.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 258: Упрощение `if` с логическим результатом**

**До рефакторинга:**

```c
#include <stdio.h>
#include <stdbool.h>

bool is_valid(int x) {
    if (x > 0) return true;
    else return false;
}

int main(void) {
    printf("%d\n", is_valid(10));
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdbool.h>

bool is_valid(int x) {
    return x > 0;
}

int main(void) {
    printf("%d\n", is_valid(10));
    return 0;
}
```

**Описание:**
Функции, возвращающие логическое значение, можно упростить до выражения, возвращающего `true` или `false` напрямую.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 259: Использование `const` в указателях на структуру**

**До рефакторинга:**

```c
#include <stdio.h>

typedef struct {
    int x;
    int y;
} Point;

void print_point(Point *p) {
    printf("%d %d\n", p->x, p->y);
}

int main(void) {
    Point pt = {1, 2};
    print_point(&pt);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef struct {
    int x;
    int y;
} Point;

void print_point(const Point *p) {
    printf("%d %d\n", p->x, p->y);
}

int main(void) {
    Point pt = {1, 2};
    print_point(&pt);
    return 0;
}
```

**Описание:**
Добавление `const` предотвращает случайное изменение структуры внутри функции и сообщает о намерениях.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 260: Удаление закомментированного старого кода**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    // printf("Old debug\n");
    printf("Final version\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Final version\n");
    return 0;
}
```

**Описание:**
Старые закомментированные строки загромождают код и сбивают с толку. Если они не нужны — лучше удалить.

**Оценка:**

* До: 5/10
* После: 10/10

---

## 🔧 **Пример 261: Проверка деления на ноль**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 10, b = 0;
    printf("Result: %d\n", a / b);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 10, b = 0;
    if (b != 0) {
        printf("Result: %d\n", a / b);
    } else {
        printf("Error: division by zero\n");
    }
    return 0;
}
```

**Описание:**
Без проверки деления на ноль программа может аварийно завершиться. Простой `if` предотвращает это и делает код безопасным.

**Оценка:**

* До: 2/10
* После: 10/10

---

## 🔧 **Пример 262: Использование `typedef` для структур**

**До рефакторинга:**

```c
#include <stdio.h>

struct Point {
    int x;
    int y;
};

int main(void) {
    struct Point p = {1, 2};
    printf("%d %d\n", p.x, p.y);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef struct {
    int x;
    int y;
} Point;

int main(void) {
    Point p = {1, 2};
    printf("%d %d\n", p.x, p.y);
    return 0;
}
```

**Описание:**
`typedef` упрощает использование структур и повышает читаемость, особенно в больших кодовых базах.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 263: Проверка `fclose()` на ошибки**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "w");
    fprintf(f, "data\n");
    fclose(f);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "w");
    if (!f) return 1;

    fprintf(f, "data\n");

    if (fclose(f) != 0) {
        perror("fclose");
        return 1;
    }
    return 0;
}
```

**Описание:**
Ошибки при `fclose` могут означать, что данные не были записаны. Проверка важна при работе с файлами.

**Оценка:**

* До: 5/10
* После: 10/10

---

## 🔧 **Пример 264: Использование `snprintf` вместо `sprintf`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char buffer[10];
    sprintf(buffer, "Number: %d", 12345);
    printf("%s\n", buffer);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char buffer[10];
    snprintf(buffer, sizeof(buffer), "Number: %d", 12345);
    printf("%s\n", buffer);
    return 0;
}
```

**Описание:**
`sprintf` может вызвать переполнение буфера. `snprintf` ограничивает длину и предотвращает уязвимости.

**Оценка:**

* До: 4/10
* После: 10/10

---

## 🔧 **Пример 265: Использование перечислений (`enum`) вместо магических чисел**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int mode = 2;
    if (mode == 1) {
        printf("Read\n");
    } else if (mode == 2) {
        printf("Write\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef enum {
    MODE_READ = 1,
    MODE_WRITE = 2
} Mode;

int main(void) {
    Mode mode = MODE_WRITE;
    if (mode == MODE_READ) {
        printf("Read\n");
    } else if (mode == MODE_WRITE) {
        printf("Write\n");
    }
    return 0;
}
```

**Описание:**
`enum` улучшает читаемость и предотвращает использование непонятных чисел в коде.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 266: Использование `const` глобально доступных значений**

**До рефакторинга:**

```c
#include <stdio.h>

char version[] = "1.0";

int main(void) {
    printf("Version: %s\n", version);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

const char version[] = "1.0";

int main(void) {
    printf("Version: %s\n", version);
    return 0;
}
```

**Описание:**
`const` предотвращает случайное изменение значений, особенно если они доступны во многих частях программы.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 267: Использование `const` с параметром указателя**

**До рефакторинга:**

```c
#include <stdio.h>

void print(const char *str) {
    printf("%s\n", str);
}

int main(void) {
    char *msg = "Hello";
    print(msg);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print(const char * const str) {
    printf("%s\n", str);
}

int main(void) {
    const char *msg = "Hello";
    print(msg);
    return 0;
}
```

**Описание:**
`const char * const` запрещает изменение как содержимого, так и адреса. Это помогает избежать ошибок и делает намерения ясными.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 268: Переход на `size_t` при работе с массивами**

**До рефакторинга:**

```c
#include <stdio.h>

void print(int arr[], int len) {
    for (int i = 0; i < len; i++) {
        printf("%d ", arr[i]);
    }
}

int main(void) {
    int data[] = {1,2,3};
    print(data, 3);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void print(int arr[], size_t len) {
    for (size_t i = 0; i < len; i++) {
        printf("%d ", arr[i]);
    }
}

int main(void) {
    int data[] = {1,2,3};
    print(data, sizeof(data)/sizeof(data[0]));
    return 0;
}
```

**Описание:**
Использование `size_t` более корректно при работе с размерами массивов, особенно на платформах с 64-битными адресами.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 269: Проверка `scanf` на успешное считывание**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int value;
    scanf("%d", &value);
    printf("You entered: %d\n", value);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int value;
    if (scanf("%d", &value) != 1) {
        fprintf(stderr, "Invalid input\n");
        return 1;
    }
    printf("You entered: %d\n", value);
    return 0;
}
```

**Описание:**
`scanf` может вернуть 0 или EOF при ошибке. Проверка результата — обязательная мера безопасности.

**Оценка:**

* До: 4/10
* После: 10/10

---

## 🔧 **Пример 270: Использование `exit()` вместо `return` из `main`, при необходимости отчистки**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Error occurred\n");
    return 1;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    printf("Error occurred\n");
    exit(EXIT_FAILURE);
}
```

**Описание:**
`exit()` полезен, если нужно выполнить действия выхода вне `main`, или если вы хотите выразить намерение завершения явнее.

**Оценка:**

* До: 6/10
* После: 8/10

---

## 🔧 **Пример 271: Использование `sizeof(var)` вместо `sizeof(type)`**

**До рефакторинга:**

```c
#include <stdlib.h>
#include <stdio.h>

int main(void) {
    int *arr = malloc(10 * sizeof(int));
    if (!arr) return 1;
    free(arr);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdlib.h>
#include <stdio.h>

int main(void) {
    int *arr = malloc(10 * sizeof(*arr));
    if (!arr) return 1;
    free(arr);
    return 0;
}
```

**Описание:**
Использование `sizeof(*arr)` вместо `sizeof(int)` делает код менее подверженным ошибкам при смене типа переменной — это более гибкий и надёжный подход.

**Оценка:**

* До: 7/10
* После: 10/10

---

## 🔧 **Пример 272: Использование явного сравнения с NULL**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char *p = NULL;
    if (p) {
        printf("Pointer is valid\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char *p = NULL;
    if (p != NULL) {
        printf("Pointer is valid\n");
    }
    return 0;
}
```

**Описание:**
Явное сравнение `p != NULL` повышает читаемость и понятность кода, особенно для начинающих разработчиков.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 273: Минимизация дублирования кода через макрос**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Error in module A\n");
    printf("Error in module B\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define PRINT_ERROR(module) printf("Error in module %s\n", module)

int main(void) {
    PRINT_ERROR("A");
    PRINT_ERROR("B");
    return 0;
}
```

**Описание:**
Макросы позволяют избежать дублирования шаблонных строк и упрощают изменение формата сообщения в будущем.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 274: Использование `bool` вместо `int` для логики**

**До рефакторинга:**

```c
#include <stdio.h>

int is_positive(int x) {
    return x > 0;
}

int main(void) {
    int result = is_positive(5);
    if (result) printf("Positive\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdbool.h>

bool is_positive(int x) {
    return x > 0;
}

int main(void) {
    if (is_positive(5)) {
        printf("Positive\n");
    }
    return 0;
}
```

**Описание:**
Использование `bool` и `<stdbool.h>` делает логику яснее, особенно когда значение — это истина/ложь, а не число.

**Оценка:**

* До: 7/10
* После: 10/10

---

## 🔧 **Пример 275: Вынос повторяющейся строки в `const` переменную**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("User not found\n");
    printf("User not found\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    const char *msg = "User not found";
    printf("%s\n", msg);
    printf("%s\n", msg);
    return 0;
}
```

**Описание:**
Вынос строки в переменную повышает читаемость и упрощает редактирование сообщения — не нужно менять в нескольких местах.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 276: Использование тернарного оператора для простых условий**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 5;
    const char *result;
    if (x > 0)
        result = "positive";
    else
        result = "non-positive";
    printf("%s\n", result);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 5;
    const char *result = (x > 0) ? "positive" : "non-positive";
    printf("%s\n", result);
    return 0;
}
```

**Описание:**
Тернарный оператор отлично подходит для компактной записи простых условных присваиваний, улучшая лаконичность кода.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 277: Использование `getchar()` вместо `scanf()` для одного символа**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char c;
    scanf(" %c", &c);
    printf("You typed: %c\n", c);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char c = getchar();
    printf("You typed: %c\n", c);
    return 0;
}
```

**Описание:**
Для считывания одного символа `getchar()` проще и надёжнее, чем `scanf`, и не требует обработки форматов.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 278: Использование `fputs` вместо `fprintf` для простых строк**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    fprintf(stdout, "Hello, world!\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    fputs("Hello, world!\n", stdout);
    return 0;
}
```

**Описание:**
Если не требуется форматирования, `fputs` быстрее и проще — хорошая альтернатива `fprintf`.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 279: Использование `memset` для инициализации структуры**

**До рефакторинга:**

```c
#include <stdio.h>

typedef struct {
    int x;
    int y;
} Point;

int main(void) {
    Point p;
    p.x = 0;
    p.y = 0;
    printf("%d %d\n", p.x, p.y);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

typedef struct {
    int x;
    int y;
} Point;

int main(void) {
    Point p;
    memset(&p, 0, sizeof(p));
    printf("%d %d\n", p.x, p.y);
    return 0;
}
```

**Описание:**
`memset` удобен для быстрой инициализации всей структуры нулями, особенно когда много полей.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 280: Инициализация массива при объявлении**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5];
    for (int i = 0; i < 5; i++) {
        arr[i] = 0;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[5] = {0};
    return 0;
}
```

**Описание:**
Инициализация массива при объявлении более лаконична и безопасна, чем явный цикл.

**Оценка:**

* До: 6/10
* После: 10/10

---

## 🔧 **Пример 281: Проверка возвращаемого значения `malloc`**

**До рефакторинга:**

```c
#include <stdlib.h>
#include <string.h>

int main(void) {
    char *data = malloc(100);
    strcpy(data, "Hello");
    free(data);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdlib.h>
#include <string.h>
#include <stdio.h>

int main(void) {
    char *data = malloc(100);
    if (!data) {
        fprintf(stderr, "Memory allocation failed\n");
        return 1;
    }
    strcpy(data, "Hello");
    free(data);
    return 0;
}
```

**Описание:**
`malloc` может вернуть `NULL`. Проверка гарантирует, что программа не будет обращаться к невыделенной памяти — это критично для стабильности.

**Оценка:**

* До: 5/10
* После: 10/10

---

## 🔧 **Пример 282: Использование `strncpy` вместо `strcpy`**

**До рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char buffer[10];
    strcpy(buffer, "This is too long!");
    printf("%s\n", buffer);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char buffer[10];
    strncpy(buffer, "This is too long!", sizeof(buffer) - 1);
    buffer[sizeof(buffer) - 1] = '\0';
    printf("%s\n", buffer);
    return 0;
}
```

**Описание:**
`strncpy` защищает от переполнения буфера. Обязательное обнуление последнего байта делает поведение безопасным.

**Оценка:**

* До: 3/10
* После: 9/10

---

## 🔧 **Пример 283: Удаление ненужной переменной**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 10;
    int result = x * 2;
    printf("%d\n", result);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int x = 10;
    printf("%d\n", x * 2);
    return 0;
}
```

**Описание:**
Удаление промежуточных переменных упрощает код, если они не переиспользуются. Это уменьшает объем памяти и делает код чище.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 284: Замена магического числа на именованную константу**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int speed = 60;
    printf("Speed limit is %d\n", speed);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define SPEED_LIMIT 60

int main(void) {
    printf("Speed limit is %d\n", SPEED_LIMIT);
    return 0;
}
```

**Описание:**
Магические числа делают код непонятным. Именованные константы документируют смысл значений и упрощают поддержку.

**Оценка:**

* До: 6/10
* После: 10/10

---

## 🔧 **Пример 285: Использование `enum` для состояний**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int state = 2;
    if (state == 1) printf("Loading\n");
    else if (state == 2) printf("Running\n");
    else if (state == 3) printf("Stopped\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

typedef enum {
    STATE_LOADING = 1,
    STATE_RUNNING,
    STATE_STOPPED
} State;

int main(void) {
    State state = STATE_RUNNING;
    if (state == STATE_LOADING) printf("Loading\n");
    else if (state == STATE_RUNNING) printf("Running\n");
    else if (state == STATE_STOPPED) printf("Stopped\n");
    return 0;
}
```

**Описание:**
Использование `enum` делает состояния программы очевидными и предотвращает ошибки при сравнении с несуществующими значениями.

**Оценка:**

* До: 6/10
* После: 10/10

---

## 🔧 **Пример 286: Замена `while(1)` на понятную конструкцию**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    while (1) {
        printf("Working...\n");
        break;
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdbool.h>

int main(void) {
    bool running = true;
    while (running) {
        printf("Working...\n");
        running = false;
    }
    return 0;
}
```

**Описание:**
`while(1)` часто используется, но делает намерения неясными. Переменная-состояние делает поведение цикла читаемым и управляемым.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 287: Использование `break` в `switch`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int n = 1;
    switch (n) {
        case 1:
            printf("One\n");
        case 2:
            printf("Two\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int n = 1;
    switch (n) {
        case 1:
            printf("One\n");
            break;
        case 2:
            printf("Two\n");
            break;
    }
    return 0;
}
```

**Описание:**
Отсутствие `break` может вызвать неожиданное выполнение следующих веток. Это распространенная и опасная ошибка в `switch`.

**Оценка:**

* До: 4/10
* После: 10/10

---

## 🔧 **Пример 288: Использование `NULL` вместо `0` для указателей**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int *ptr = 0;
    if (ptr == 0) {
        printf("Null pointer\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stddef.h>

int main(void) {
    int *ptr = NULL;
    if (ptr == NULL) {
        printf("Null pointer\n");
    }
    return 0;
}
```

**Описание:**
`NULL` явно указывает, что значение — указатель, а не число. Это повышает читаемость и предотвращает недопонимание.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 289: Разделение длинной функции**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int a = 5, b = 3;
    int sum = a + b;
    int diff = a - b;
    int prod = a * b;
    printf("Sum: %d\n", sum);
    printf("Diff: %d\n", diff);
    printf("Prod: %d\n", prod);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void calculate(int a, int b) {
    printf("Sum: %d\n", a + b);
    printf("Diff: %d\n", a - b);
    printf("Prod: %d\n", a * b);
}

int main(void) {
    calculate(5, 3);
    return 0;
}
```

**Описание:**
Вынос логики из `main` упрощает тестирование, повторное использование и повышает читаемость.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 290: Использование макроса для определения размера массива**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    int arr[] = {1, 2, 3, 4};
    size_t len = sizeof(arr) / sizeof(arr[0]);
    printf("Length: %zu\n", len);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

#define ARRAY_SIZE(arr) (sizeof(arr) / sizeof((arr)[0]))

int main(void) {
    int arr[] = {1, 2, 3, 4};
    printf("Length: %zu\n", ARRAY
```

---

## 🔧 **Пример 291: Улучшение читаемости через логическое объединение условий**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    char c = 'a';
    if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u') {
        printf("Vowel\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char c = 'a';
    if (strchr("aeiou", c)) {
        printf("Vowel\n");
    }
    return 0;
}
```

**Описание:**
Использование `strchr()` упрощает проверку вхождения символа в набор. Такой подход более читаемый и масштабируемый.

**Оценка:**

* До: 6/10
* После: 9/10

---

## 🔧 **Пример 292: Использование `goto` для выхода из вложенных блоков (в редких случаях)**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "r");
    if (f) {
        char buf[100];
        if (fgets(buf, 100, f)) {
            printf("Read: %s\n", buf);
        } else {
            fclose(f);
            return 1;
        }
        fclose(f);
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "r");
    if (!f) return 1;

    char buf[100];
    if (!fgets(buf, sizeof(buf), f)) {
        goto cleanup;
    }

    printf("Read: %s\n", buf);

cleanup:
    fclose(f);
    return 0;
}
```

**Описание:**
В некоторых ситуациях `goto` упрощает выход из нескольких уровней вложенности и помогает избежать дублирования кода, например, закрытия файла.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 293: Замена `feof` + `fgets` на проверку результата `fgets`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "r");
    char buf[128];
    while (!feof(f)) {
        fgets(buf, 128, f);
        printf("%s", buf);
    }
    fclose(f);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "r");
    char buf[128];
    while (fgets(buf, sizeof(buf), f)) {
        printf("%s", buf);
    }
    fclose(f);
    return 0;
}
```

**Описание:**
Использование `feof` до чтения — ошибка. `fgets` возвращает `NULL` при конце файла, что и следует проверять.

**Оценка:**

* До: 4/10
* После: 10/10

---

## 🔧 **Пример 294: Использование `const` в параметре функции**

**До рефакторинга:**

```c
#include <stdio.h>

void greet(char *name) {
    printf("Hello, %s!\n", name);
}

int main(void) {
    greet("Alice");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

void greet(const char *name) {
    printf("Hello, %s!\n", name);
}

int main(void) {
    greet("Alice");
    return 0;
}
```

**Описание:**
Если функция не изменяет строку, `const` в параметрах защищает от ошибок и улучшает читаемость.

**Оценка:**

* До: 7/10
* После: 10/10

---

## 🔧 **Пример 295: Проверка возвращаемого значения `fopen`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "r");
    char buf[100];
    fgets(buf, 100, f);
    fclose(f);
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    FILE *f = fopen("file.txt", "r");
    if (!f) {
        perror("fopen");
        return 1;
    }
    char buf[100];
    fgets(buf, sizeof(buf), f);
    fclose(f);
    return 0;
}
```

**Описание:**
Если `fopen` возвращает `NULL`, программа не должна продолжать чтение — это приведет к краху. Проверка обязательна.

**Оценка:**

* До: 4/10
* После: 10/10

---

## 🔧 **Пример 296: Использование `NULL` вместо `(void *)0`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    void *ptr = (void *)0;
    if (ptr == (void *)0) {
        printf("Null\n");
    }
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    void *ptr = NULL;
    if (ptr == NULL) {
        printf("Null\n");
    }
    return 0;
}
```

**Описание:**
`NULL` — стандартный способ задания пустого указателя в C. Использование `(void *)0` избыточно и менее читаемо.

**Оценка:**

* До: 5/10
* После: 9/10

---

## 🔧 **Пример 297: Использование `exit(EXIT_SUCCESS)` вместо `return 0`**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Done\n");
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    printf("Done\n");
    exit(EXIT_SUCCESS);
}
```

**Описание:**
`exit(EXIT_SUCCESS)` ясно показывает намерение корректного завершения, особенно полезно при написании библиотек и вложенных вызовов.

**Оценка:**

* До: 7/10
* После: 9/10

---

## 🔧 **Пример 298: Избежание повторного определения `main` в заголовках**

**До рефакторинга (заголовок):**

```c
int main(void);
```

**После рефакторинга:**

```c
// Удалено: определять main в заголовке — ошибка
```

**Описание:**
`main` никогда не должен быть объявлен в заголовочных файлах. Это приводит к конфликтам при линковке. Удаление — правильный шаг.

**Оценка:**

* До: 2/10
* После: 10/10

---

## 🔧 **Пример 299: Явное указание типа возврата `main`**

**До рефакторинга:**

```c
main() {
    return 0;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    return 0;
}
```

**Описание:**
Без `int` компилятор может не определить поведение функции `main`. Это нарушение стандарта C и снижает переносимость.

**Оценка:**

* До: 3/10
* После: 10/10

---

## 🔧 **Пример 300: Использование `fprintf(stderr, ...)` для ошибок**

**До рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    printf("Error: something went wrong\n");
    return 1;
}
```

**После рефакторинга:**

```c
#include <stdio.h>

int main(void) {
    fprintf(stderr, "Error: something went wrong\n");
    return 1;
}
```

**Описание:**
Сообщения об ошибках должны направляться в `stderr`, чтобы не смешиваться с обычным выводом, особенно при перенаправлении.

**Оценка:**

* До: 5/10
* После: 10/10

---
